# A4 · Motor deportivo, catálogo y seguridad clínica — Diego Ramírez, Director Deportivo

## Veredicto en una frase

El catálogo de 374 (los 127 nuevos incluidos) está limpio y el filtro de dolor por reporte del asesorado protege las 10 zonas que promete, pero hay dos puertas abiertas reales: el campo donde el propio coach anota la lesión al dar de alta solo protege 3 de esas 10 zonas, y el candado que corrige ejercicios avanzados en planes de principiante existe, funciona cuando lo pruebo a mano, y aun así hay principiantes e intermedios reales entrenando avanzado hoy.

## Los 3 más grandes

1. 🔴 **Un principiante lleva semanas con "Ab Wheel" y "Pike Push-up" — los dos ejercicios que el propio catálogo marca como no aptos para su nivel — y el arreglo que existe para esto no se aplicó.**
   - Evidencia: `user_data` de FELIPE R.L (`c6c46e88…`, `level:'Principiante'`, `place:'corporal'`). Sus rutinas «Full Body» y «Full Body 3» (`generated:true`) tienen `e97` (Pike Push-up) y `e47` (Ab Wheel) — ambos con nivel `'A'` en `EX_LEVEL` (avi-core.js:1421-1426), y `e47` dice en su propia descripción: *«No apto para principiantes sin base de core»* (app-1-infra.js). `profile->>'levelHealed'` = `null` — el sanador nunca corrió sobre este cliente. Segundo caso: Sofía Vega Triana (Intermedio) tiene `e92` «Hip Thrust Unilateral» (`generated:true`, `levelHealed` no presente) — es exactamente el ejercicio que el propio changelog de v536 cita como el caso que la re-etiquetación de v513 dejó vivo en planes viejos. Tercer caso (fuera del alcance del sanador, hecho a mano por el coach): Kathe Beltran (Intermedio) tiene `e69` «Clean & Press ⚠️ AVANZADO» — el nombre lleva la advertencia en el propio catálogo.
   - Intenté tumbarlo así: corrí `healRoutineLevel(client, DB.exercises)` (avi-core.js:1587) a mano contra la rutina real de FELIPE, tal como Supabase la tiene guardada hoy. La función SÍ funciona: reemplaza `e97`→`e109` (Elevaciones Y-T-W) y `e47`→`e132` (Elevación de Piernas Tumbado), ambos dentro de su nivel y su entorno (`corporal`). El código está bien. `_curarNivelDeLosPlanes()` (app-3-coach.js:773) SÍ está cableada al arranque del panel del coach (`_hydrateCoachFromRows`, línea 758) y barre TODOS los clientes cada vez que él abre el panel. Con la app en v563 y el coach usándola a diario, el sanador debería haber corrido docenas de veces desde que v536 salió — y sin embargo el dato en la nube sigue sucio.
   - Qué cuesta arreglarlo: no es el motor — es diagnosticar por qué la escritura de `_curarNivelDeLosPlanes` no persiste (¿un error silencioso? ¿el teléfono del propio asesorado la vuelve a ensuciar en cada sync, como pasó con el `env` en v517?). Recomiendo instrumentar esa función (contar cuántas veces corre y cuántas cambia algo) antes de tocarla a ciegas. Mientras tanto: revisar a mano los planes de FELIPE, Sofía y Kathe.

2. 🔴 **El campo donde el coach anota la lesión al dar de alta —el que la propia doctrina le pide usar primero— solo protege 3 de las 10 zonas del dictamen de Laura.**
   - Evidencia: `parseLimitations(notes)` (avi-core.js:933, `GEN_LIMIT_KWS` línea 326) solo reconoce `rodilla`, `lumbar`, `hombro` y un `generic` que detecta pero no excluye nada. Probé con texto real de intake: `parseLimitations('Le duele el codo al hacer extensiones')` → `{detected:false, keys:[]}`. Mismo resultado con `muñeca`, `pecho`, `cuello`, `tobillo`, `aductor/ingle`. El texto de control (`hernia lumbar L5`, el caso real de Danilo) sí detecta. Las 7 zonas mudas SÍ tienen regla de exclusión completa en `GEN_ZONE_EXCL` (codo/muñeca/pecho llegaron con el dictamen de Laura del 27-ago, v546) — pero solo se activan si es el ASESORADO quien las reporta desde el flujo de "Reportar dolor" dentro del modo guiado (`gmReportPain`, app-6-extra.js:759), que exige que la persona ya esté entrenando y sintiendo dolor EN ESE MOMENTO. No existe ningún formulario para que el coach registre una lesión de codo/muñeca/pecho/cuello/tobillo/cadera al dar de alta a alguien — su único campo es `notes`, y `notes` no las entiende.
   - Intenté tumbarlo así: busqué en toda la base si algún cliente real tiene una de esas 7 palabras en sus notas — hoy no hay ninguno (los 2 casos reales con lesión declarada, Danilo y Miguel, caen en lumbar y rodilla, que SÍ están cubiertas). No hay una víctima hoy. Pero el protocolo clínico de `CLAUDE.md` (§PROTOCOLO — ASESORADO CON LIMITACIÓN FÍSICA, Paso 1) le pide al equipo recolectar "qué articulación" ANTES de diseñar, y el único sitio donde eso queda escrito en la app de producción es ese campo mudo para 7 de 10 zonas.
   - Qué cuesta arreglarlo: dos caminos, decisión del PO. (a) barato: ampliar `GEN_LIMIT_KWS` con los mismos términos que ya existen en `GEN_ZONE_EXCL` para las 7 zonas que faltan (las regex ya están escritas, es reutilizarlas); (b) mejor a mediano plazo: darle al coach el mismo formulario estructurado de 16 zonas que usa el asesorado, para que la información no dependa de que él escriba la palabra exacta en texto libre.

3. 🟡 **Un ejercicio nuevo del lote 4 (plancha con disco) rompe la promesa "hoy trabajamos sin carga" que v553 acababa de arreglar para toda la clase.**
   - Evidencia: `e365` «Plancha con Disco en la Espalda» es `type:'Isométrico'` → `exTrack` lo manda a `track:'tiempo'`. `_isLoadedEx(ex)` (avi-core.js:3933) solo considera "carga" a `track==='peso_reps'`, así que cuando alguien marca 🤕 "Con dolor", `_demoteToBodyweight` nunca lo toca. Lo comprobé corriendo `applyMood({exercises:[e365]}, 'dolor', {})`: el texto que recibe la persona dice *"Hoy trabajamos suave, sin carga"* y el ejercicio sale intacto — 3 series, disco sobre la zona lumbar, sin tocar. El propio test que blinda v553 (avi.test.js:4596, "un isométrico no es carga: el ajuste no puede tocarlo") usa como control una plancha SIN peso — nunca contempló que el catálogo trajera una isométrica CON peso, porque `e365` entró después, en el mismo lote 4.
   - Intenté tumbarlo así: confirmé por SQL que `e365` no está hoy en ningún plan real (0 filas) — es una mina sin pisar, no una víctima. Pero es exactamente la misma clase de bug que v553 cerró para 23 ejercicios (Funcional/core cargado), reabierta por el lado del track `tiempo`.
   - Qué cuesta arreglarlo: `_isLoadedEx` necesita una segunda condición para isométricos con peso declarado (o Laura decide que la plancha con disco simplemente no se asigna a nadie con lesión activa hasta que se resuelva). Unas líneas.

## Todos los hallazgos

(Los 3 de arriba, más lo verificado sano — ver siguiente sección — no encontré un cuarto hallazgo con evidencia suficiente para el tiempo que tenía. Preferí tres hallazgos ciertos a rellenar con hallazgos menores.)

## Lo que verifiqué y está SANO (con números)

- **Catálogo nuevo (e255-e381, los 127 del lote v547-v550): 127/127 completos.** Los 5 campos obligatorios (`muscle`, `type`, `sets`, `reps`, `env`, `level`, `desc`, `descSimple`, `muscleLabel`) están presentes en los 127 — cero fichas mudas. `audit-catalog.mjs`: 0 BLOCK sobre el catálogo entero (los 142 MAJOR son "sin foto", ya documentado y decisión pendiente del PO, no un defecto de este delta).
- **Mapa muscular: 127/127 mapeados en `MM_EX`, cero con `p` (músculo principal) vacío**, y verifiqué que cada región que usan (`p`+`s`) existe de verdad como prefijo válido en `MM_FRONT`/`MM_BACK` — cero regiones fantasma que dejarían el SVG en blanco.
- **`type` como modalidad: v554 se sostiene.** Verifiqué en el código actual que `e369`/`e370`/`e371` (crunch bicicleta, V-ups, plancha lateral con elevación) siguen como `Bodyweight`, no `Aislamiento` — el fix de v554 no se revirtió. Barrí los 127 buscando otros posibles "pide kilos sin cargar / carga sin pedir kilos" cruzando `type` contra palabras de peso en el nombre/descripción (disco, mancuerna, barra, chaleco, kettlebell) — el único caso real es `e365` (hallazgo #3, arriba); el resto usa `reps` con "lastre opcional" correctamente para lo que sí es peso corporal con carga añadida opcional (ej. `e347` sentadilla con chaleco lastrado).
- **Nivel y entorno de los 127: sin huecos.** Los 127 traen `level` explícito (`P`/`I`) y `env` no vacío — ninguno cae al comodín "solo gimnasio" del hueco que v516 cerró.
- **Barrido de zonas de lesión sobre el catálogo completo: verificado que el mecanismo del lote 4 (e358-e381, core/cardio) SÍ tiene sus exclusiones por ID donde el nombre no delata** (`e360`/`e361` leñador, `e364` elevación de rodillas colgado, `e367` dragon flag → lumbar por ID, documentado y con control en el propio código). No encontré ningún id de e255-e381 que debiera caer en una de las 10 zonas y no cayera, más allá del hallazgo #3 (que es de modalidad, no de exclusión por zona).
- **Filtro de dolor estructurado (el que usa el asesorado desde el modo guiado): las 15 zonas con regla de exclusión SÍ están completas** (`_PAIN_ZONE_TO_EXCL`, avi-core.js:7995) incluidas codo/muñeca/pecho del dictamen del 27-ago. `otra zona` sigue sin lista a propósito (decisión de Laura, documentada, no un hueco).
- **Suite completa: 972/972 verde**, igual que el baseline del briefing. No introduje ni encontré ninguna regresión.

## Sospechas sin medir

- **No pude confirmar la causa raíz de por qué `_curarNivelDeLosPlanes` no persistió para FELIPE/Sofía** (hallazgo #1) — solo confirmé que el código funciona en aislamiento y que los datos en producción siguen sucios. Podría ser un error silencioso, una condición de carrera con el teléfono del propio asesorado (patrón ya visto en v517 con `env`), o que el coach no haya vuelto a cargar el panel completo de clientes desde que v536 salió. Necesita instrumentación, no until until una hipótesis a ciegas.
- **No revisé si hay más clientes con exercises nivel `'A'`** más allá de los 25 reales de la base — solo crucé Principiante e Intermedio contra `EX_LEVEL`; no repetí el barrido de "5.760 planes" que menciona el changelog de v513 (esa cifra sale del generador puro, no de datos reales; mi verificación fue sobre datos reales, que es lo que pide el briefing).
- **No verifiqué si `e97`/`e47` (Felipe) llegaron por generación automática original o por edición manual del coach después.** Si el coach los añadió a mano a propósito (algo defendible si Felipe ya tenía base), el hallazgo #1 seguiría siendo cierto para Sofía (e92, `generated:true`, sin editar) pero el de Felipe pasaría de "el sanador falló" a "es una elección del coach que el sanador no debe tocar" — la ficha `levelHealed:null` no distingue entre las dos causas.

## Qué NO miré y por qué

- **v529/530 (umbral de consolidación de peso), v532 (descarga programada), v536 (motor de re-etiquetado hacia adelante) y v545 (tiempo del HIIT derivado del protocolo): no los re-audité desde cero.** Cada uno tiene su propia matriz de sabotaje documentada en el repo (`_sabotaje-deload`, `_verify-deload`, `_sabotaje-hiit`, etc.), todas verdes en la suite de 972, y sus gotchas describen el razonamiento con números medidos por quien los construyó. Verificar esos cuatro mecanismos desde cero con el mismo rigor que le di al catálogo y al filtro de dolor habría consumido el presupuesto entero de esta sesión sin dejar tiempo para lo que el briefing marcó como razón nº1 de mi área (los 127 ejercicios nuevos). Los tomé como sanos por evidencia indirecta (suite verde + sabotaje documentado), no por haberlos vuelto a medir yo mismo — es una limitación real, no una negación.
- **No probé el generador con semillas nuevas para ver si el catálogo +127 produce repeticiones dentro de la semana en escenarios sintéticos.** En su lugar medí sobre los 25 clientes REALES de producción (más cercano a lo que el briefing pide) y solo encontré un caso de repetición alta (FELIPE R.L., `e107`/`e82` en 4 de 4 días) — que corresponde a un Full Body de peso corporal con pool chico, no a un defecto nuevo del catálogo ampliado. No corrí el barrido de 40 semillas que la doctrina recomienda para separar "pool chico real" de "cursor del generador mal alineado" — me faltó tiempo.
- **No audité Nutrición**, por instrucción explícita del briefing (el PO la dio por cerrada). No encontré nada en mi recorrido que le hiciera daño físico a alguien, así que no hay nada que reportar ahí.
- **No probé en un iPhone real** ni en ningún dispositivo — trabajé enteramente contra el código y contra `user_data` vía SQL de solo lectura. Cualquier defecto que dependa de renderizado/WebKit queda fuera de mi alcance.
- **No revisé Comunidad** (0 menciones también en mi área, como señaló el briefing) — no vi nada en el motor deportivo que la tocara.
- **No repetí `scripts/fugas-zonas.mjs`/`probar-zona.mjs` como scripts ejecutables** — usé sus mismas ideas (comparar catálogo vs. nombre guardado en el plan) directamente contra los 2 casos reales de limitación declarada en `notes` (Danilo, Miguel), que están cubiertos. No hay hoy ningún cliente real con una lesión de codo/muñeca/pecho/cuello/tobillo/cadera declarada en `notes` para poder medir una fuga EN VIVO del hallazgo #2 — la evidencia que tengo es que el detector no reconoce esas palabras, no que le esté fallando a una persona real hoy mismo.
