# 📜 AVI — Bitácora de versiones y sesiones

> Historial COMPLETO movido desde CLAUDE.md el 2026-07-07 (tarea #6: adelgazar el contexto
> vivo). Dos partes: el roadmap histórico por versión y los hitos crudos por sesión (más
> reciente primero). Las lecciones que no expiran están destiladas en CLAUDE.md → GOTCHAS VIGENTES.

## Roadmap histórico (v1.0 → v1.5, bloques ✅)

### ✅ v1.0 — Base (2025)
- Arquitectura single-file PWA + Supabase sync
- Login dual coach/asesorado
- Constructor de rutinas con ejercicios
- Historial de entrenamientos

### ✅ v1.1 — Funcionalidades core
- Push notifications con VAPID real (Edge Function send-push)
- Sistema de mensajes coach ↔ asesorado
- Fotos de progreso, medidas corporales, peso corporal
- Plantillas de rutinas reutilizables
- PWA instalable (manifest + SW)
- TWA nativo para Android (assetlinks.json)

### ✅ v1.2 — Negocio y contenido
- Sistema completo de pagos y membresía (estados, fechas manuales)
- Dashboard analytics del coach (MRR, activos, retención SVG)
- Plan nutricional completo (macros automáticos, templates, exportar PNG)
- 88 ejercicios con validación Coach Pro
- RCT e ICC reemplazando IMC (métricas con evidencia)
- Racha de entrenamiento semanal
- Alerta de inactividad automática
- Asesorado puede elegir cualquier rutina

### ✅ v1.3 — Calidad y UX (Mayo 2026)
- Dark mode automático + toggle manual (☀️/🌙/⚙️)
- Hardening de seguridad: XSS, canLogin, SHA-256, send-push auth
- Pre-commit hook — 7 checks automáticos en cada git commit
- Pipeline de deploy con gates obligatorios Lucas QA + Julián QA
- Campo `phone` en asesorado para WhatsApp directo
- WhatsApp reminder mejorado (monto + fecha + CTA urgente)
- Banner proactivo de vencimientos en Home (≤5 días)
- History limit 60 → 365 sesiones
- `delClient()` limpia todos los datos huérfanos (nutrition, medidas, photos)
- exportData() exporta las 10 colecciones
- 7 textos humanizados (Sofía): loading, sync-dot, toasts, push title, wizards
- Tokens CSS `--surface` y `--accent3` en los 3 bloques `:root`
- Touch targets ≥36px (WCAG 2.5.5)
- Experiencia primer día asesorado (Mayo 2026): badge login removido, hint de credenciales, estados vacíos accionables con CTA a mensajes, día de descanso con mensaje maduro, botón "Empezar" renombrado, pills de perfil con guard, mensaje fin de sesión orientado al progreso, typo Bíceps, warmup simplificado, onboarding Slide 1 recontextualizado, push delay 4s

### ✅ v1.3.1 — Notificaciones + Marca (Mayo 2026)
- Notificaciones diarias personalizadas: 7am / 10am / 5pm (Colombia UTC-5)
- Edge Function `daily-notifs` desplegada en Supabase — 3 cron jobs activos
- Mensajes según día de entreno (training_days) y turno por rutina (training_shift JSONB)
- 4 variantes del mensaje de 5pm: post-workout / pre-workout / noche / neutral
- Pool de 7 mensajes por franja, rotación por día de semana
- Formulario de rutina: campo "Horario habitual" (rf-shift) al lado de "Día asignado"
- Fix instalación PWA: instrucciones permanentes en login para iOS y Android
- Rebranding: emoji 💪 eliminado del wordmark, nuevo tagline en evaluación
- Agente Lucía Brand & Copy Strategist creado (.claude/agents/lucia-brand.md)
- Tagline actual: "Entrenamiento con tu coach, en tu bolsillo" (en revisión)
- Candidatos evaluados por Lucía: "Tu coach real, en tu celular" / "Entrena con quien sabe"

### ✅ v1.3.1 — Sesión 2026-05-24 (rutinas + fixes)
- Rutinas personalizadas creadas para 5 asesorados vía Python → Supabase directo
- Criterio de orden en rutinas: **Compuesto → Funcional → Aislamiento → Cardio** (regla permanente)
- Criterio de diseño: mujeres → glúteo+piernas primero; hombres → tren superior; menores 16 → sin carga axial
- 4 ejercicios nuevos: Remo Australiano (fb01), Swing Mancuerna (fb02), Thruster (fb03), PM Rumano Mancuernas (fb04)
- Fix: `capture="environment"` eliminado de `dob-photo-input` — asesorado ya puede elegir galería en onboarding
- agent-browser instalado globalmente (`~/.claude/agents/agent-browser.md`) + Chrome 149 en `~/.agent-browser/`
- Pipeline de rutinas reutilizable en `C:\Users\KRONOS\AppData\Local\Temp\crear_rutinas.py`

### ✅ v1.3.2 — Sesión 2026-05-25 (push notifications + rutinas + seguridad)

**Push notifications — causa raíz resuelta:**
- Blob URL SW no registra en Android Chrome moderno → `navigator.serviceWorker.ready` nunca resuelve → push falla
- Fix definitivo: `sw.js` ahora es archivo estático registrado como `/apex-app/sw.js` con scope `/apex-app/`
- `subscribePush` refactorizado para ser idempotente: `window._swReg || await navigator.serviceWorker.ready` → reusa suscripción existente sin `unsubscribe()`
- Botón "Reactivar push" en modal de notificaciones (cuando permiso ya fue concedido)
- `tryAutoLogin` llama `subscribePush` con delay 3s (coach) / 4s (cliente) — siempre con guard de permiso

**Rutinas — auditoría y corrección completa (8 asesorados):**
- 3 asesorados nuevos detectados: Astrid Beltran, Nataly, Cristian Calderon
- Formato roto `{exId, reps:"10-12"}` corregido en: Astrid (Jueves), Nataly (Ma/Ju/Vi), Cristian (todos)
- `reps` como string (`"12"`, `"1"`, `"20"`) corregido a number en Andrés Martínez y Astrid Beltran
- Fix `daily-notifs/index.ts` línea 235: icon path `/icons/icon-192.png` → `/apex-app/icons/icon-192.png`

**Seguridad (hardening sin refactor arquitectural):**
- XSS cerrado en 10 puntos: nutrición (coach+cliente), perfil, rutinas cliente, progreso ejercicios, notificaciones programadas, fotos
- `photo.src` validado como `data:image/...` antes de insertar en `<img>`
- Sesión con expiración: `expiresAt: now + 30 días` al hacer login; `tryAutoLogin` lo valida
- CORS `*` → `https://kronos-apex.github.io` en `send-push` y `daily-notifs`

### Asesorados actuales y sus rutinas (2026-05-25)
| Nombre | Sexo | Edad | Nivel | Días/sem | Objetivo | Rutinas | Push sub |
|---|---|---|---|---|---|---|---|
| Kathe Beltran | F | 28 | Principiante | 4 | Perder grasa | Lu/Ma/Ju/Vi | ❌ |
| Samuel Cifuentes | M | 14 | Principiante | 3 | Perder grasa | Full Body Lu/Mi/Vi — sin carga axial | ❌ |
| Miguel Pulido | M | 29 | Intermedio | 5 | Ganar músculo | Empuje Superior (Lu) / Pierna A (Ma) / Hombros+Brazos (Mi) / Jalar Superior (Ju) / Glúteo+Bisagra+Core (Vi) — ⚠️ rodilla derecha operada, sin impacto, RDL en progresión desde patrón | ❌ |
| Andrés Martínez | M | 37 | Avanzado | 5 | Ganar músculo | Pierna/Push/Pull/Hombros+Brazos/Cardio | ✅ |
| Natalia Martinez | F | 34 | Principiante | 3 | Recomposición | Lu/Mi/Vi | ❌ |
| Astrid Beltran | F | — | Principiante | 5 | — | 5 rutinas | ❌ |
| Nataly | F | — | — | 4 | — | Lu/Ma/Ju/Vi | ❌ |
| Cristian Calderon | M | — | — | 4 | — | Lu/Ma/Ju/Vi | ❌ |

**Regla de formato de ejercicios en rutinas (CRÍTICO):**
- Formato CORRECTO: `{id, icon, name, reps (number), sets, type, muscle}`
- Formato ROTO (no renderiza): `{exId, reps: "10-12", sets, muscle, restSec}` — si aparece, hay que corregirlo
- IDs válidos: `e1`–`e88` (defaultExercises en index.html) + `fb03`/`fb04` (en Supabase ax_e)

### ✅ v1.3.3 — Sesión 2026-05-28

**Fotos → Supabase Storage (commit f261e03):**
- Bucket `apex-photos` creado en Supabase (público, 5MB, jpeg/png/webp)
- RLS policies para anon: INSERT, SELECT, DELETE
- `uploadPhotoToStorage(clientId, photoId, base64)` → retorna URL pública
- `deletePhotoFromStorage(clientId, photoId)` → borra archivo al eliminar foto
- `migratePhotosToStorage()` → migra base64 existentes en background (3s post-arranque)
- `savePhoto()` y `_dobSavePhoto()` ahora suben a Storage con fallback a base64
- `renderPhotosClient()` y `viewPhoto()` aceptan `https://` además de `data:image/`
- `deletePhoto()` limpia archivo del bucket además del registro en `ax_photos`

**Cobros por Nequi (commit 076c85c):**
- `ax_nequi` en SB_KEYS — número Nequi del coach visible para todos los clientes
- Campo Nequi en ⚙️ Configuración del coach
- `renderPaymentCard(client)` — card automática al cliente cuando plan vence en ≤7 días
- `notifyPaid()` — push a `_coach` + mensaje en chat como historial
- `copyNequi(num)` — copia al portapapeles, fallback toast

**Identidad de marca (commits a485c78, d7aa7ae):**
- Tagline confirmado: **"Entrenamiento con nombre propio"** — loading screen y login
- Íconos PWA verificados: ya tienen la Letra de Hierro (A con barra de pesas dorada)
- `BRAND.md` creado — brief completo de marca: paleta, tipografía, logo, tono, reglas

### ✅ v1.4 — Self-serve, modalidades y rediseño (Mayo–Junio 2026)

**Modalidades de entrenamiento (commits 0e0b48f, fc66b83):**
- Campo `track` por ejercicio: `peso_reps` / `reps` / `tiempo` / `cardio` / `hiit`
- Arregla el bug histórico de pedir "kg" en cardio HIIT y peso corporal
- Timer HIIT (intervalos + pitido/vibración + Wake Lock) y cronómetro isométrico
- PRs y gráficas de progreso por modalidad con unidad propia (back-compat con datos viejos en kg)
- `migrateExTypes()` reclasifica 15 defaults a Bodyweight + e74 a HIIT, una sola vez

**Auto-generador de rutinas — Paso 1 self-serve (commit 98c418e):**
- `generarRutinas()` en `avi-core.js` (función pura, testeada) + botón ✨ "Generar semana"
- Splits por sexo+días, scheme por objetivo+nivel, exclusiones por limitación física y por entorno
- El coach SIEMPRE revisa/aprueba el borrador antes de asignar (innegociable por seguridad)
- Modelo self-serve de 2 niveles definido (libre gratis + coach pago) — NO se quita el coach

**Entornos y estilos de equipo (commits 631348b, 004ef6f, 2609e23):**
- Eje `env` independiente de `goal` y `tier`: corporal / casa / parque / gym
- `inferExerciseEnv()` propone entorno por nombre+tipo; generador y picker filtran por entorno
- Selector de estilo (preset entorno+metodología) + editor de env en la UI
- +13 ejercicios sin gym para llenar el hueco de peso corporal

**Gamificación (commits 1acb042, 832b73c):**
- Nivel permanente 1–5 que no se reinicia + logros
- Descuento del mes por adherencia (5/10/15%) — visible al asesorado y al coach (informativo, se aplica a mano)

**Rediseño Noir Esmeralda (commits varios, apex-v37):**
- Marca esmeralda `#10E0A0` + oro + casi-negro; login cinematográfico con video real
- Pantalla de carga con imagen, intro editorial de la semana, bienvenida personalizada
- Tarjetas de ejercicio estilo Nike con imágenes reales + lightbox al tocar
- Ícono ala esmeralda (Noir Esmeralda); el asesorado puede poner su propia foto de perfil
- Auditoría de imágenes de ejercicios (14 corregidas / 4 quitadas)

**UX para no técnicos (commits 79e9251, d2600ac, 3bcfd96):**
- Ayuda contextual ❓ re-accesible por sección; empty states accionables
- Progressive disclosure del perfil del asesorado (oculta tarjetas vacías)
- Vista de adherencia accionable en el coach ("necesitan un empujón")

**Robustez (commits d03b343, aa46da6, 0fccb0f):**
- Boot blindado contra `avi-core.js` viejo en caché (no cuelga el arranque)
- Bloqueo de escritura a la nube desde `file://` + anti-borrado de `ax_c`/`ax_e`
- avi-core.js cubierto por los checks de sintaxis/duplicados del audit
- Suite de tests: 41 → **56 tests** (incluye generador + entornos)

### ✅ Sesión 2026-06-01 — Integridad de datos, auditoría, marca AVI

**🔴 Blindaje de pérdida de datos (incidente real: se perdieron entrenos de Nataly y Andrés Martínez):**
- `mergeHistory` / `mergeClientArrays` / `mergePRs` en `avi-core.js`: `syncFromCloud` ya NO sobrescribe `ax_hist`/`ax_m`/`ax_bw`/`ax_med`/`ax_pr` — los FUSIONA nube+local (commits 95e91cc, 8e224b3). +14 tests → **72/72**.
- Cola de reintento `_pendingPush` + `flushPendingSync()` en `online`/`pagehide`/`visibilitychange` (un envío fallido por mala señal se reintenta solo). Sin keepalive (límite 64KB).
- Guardado PARCIAL: botón "Finalizar entrenamiento" (`finishSessionEarly`) guarda aunque no se marque el 100%. `saveSessionToHistory` reconstruye snapshot siempre.
- Calentamiento PERSISTE (`wu_{routineId}_{exId}` en localStorage; antes se borraba al re-render). Commit d403eb6.
- Tarjeta **"Entrenaron hoy"** con nombres en el dashboard del coach (commit 4a7a542).
- `MS.canLogin` ahora permite `pending` (asesorado nuevo entra → onboarding + tier libre).

**🔍 Auditoría de equipo completa → `docs/auditoria-2026-06-01.md`** (seguridad/DBA, deportivo, QA, CS, ing, diseño, PM). Backlog priorizado. Crítico abierto: **RLS permisivo** = toda la DB es pública con la key anon (no se arregla sin auth real; bloqueador del arrendamiento). Arreglados: login `pending` + mensaje honesto de lesiones genéricas (`parseLimitations.hasExclusions`).

**⚖️ + 🔐 Modo libre público (v2.0) — arrancado:**
- Decisión: abrir el modo libre a personas reales (no-coaches) con **login real Supabase Auth (Google + Email)**; usuario libre independiente que luego conecta coach. Ver [[memoria estrategia self-serve]].
- Borradores legales en `legal/` (Habeas Data Ley 1581 — pendiente abogado + datos del responsable; SIN push).
- Guía de config Google en `docs/setup-login-google.md` (clics de Camilo en Google Cloud + Supabase).
- Decisión técnica pendiente: supabase-js (CDN) vs OAuth a mano (rompe "sin dependencias").

**🏷️ RENOMBRE AVI → AVI (commit 83db4ae, apex-v44):** nombre visible cambiado en toda la app (login, carga, manifest, notificaciones, imágenes, WhatsApp, gamificación). **Internos INTACTOS** (`ax_*`, `apex_data`, `avi-core.js`, `#apex-loading`, caché `apex-vNN`, repo `apex-app`) — NO renombrar. AVI = iniciales de los hijos (Alexander/Valery/Isabella). Handle @avi.entrena. Dominios libres: avi.lat + holaavi.com (sin comprar aún). Pendiente: registro SIC. La PWA instalada conserva el nombre viejo hasta reinstalar (no es bug).

### ✅ Sesión 2026-06-02 — Fixes, personalización del generador y modo libre completo (apex-v44 → apex-v55)

**🐛 Fixes (commit ea21854):**
- Barra de retención mostraba entrenos fantasma: agrupaba por día de la semana (`getDay`) en ventana de 7 días → el mismo día de la semana pasada caía en "hoy". Ahora por día de calendario real.
- Auto-guardado PARCIAL automático desde la 1ª serie marcada (antes solo al 100% o con "Finalizar") — sync con debounce.
- Cronómetro de descanso por **timestamp absoluto** + recalcula en `visibilitychange` (iOS suspende `setInterval` con pantalla bloqueada → se congelaba). Límite iOS: no suena con pantalla bloqueada (necesita push nativo).

**🧪 Refactor testeable (commit 676329a):** lógica de fechas del dashboard extraída a `avi-core.js` (era inline sin tests). Regla: funciones de fecha reciben `now`, nunca `new Date()` implícito.

**📅 Orden de días (commit acf5fe9):** rutinas Lunes→Domingo (`sortRoutinesByDay`) + migración de arranque + ordena al crear/editar/generar.

**🌱 Fase de adaptación (commit 925c542) + ⚖️ composición IMC/cintura (commit e996e2f):** ver sección "Auto-generador". Decisión metodológica: full body para principiantes es correcto; lo que faltaba era el esquema de carga (adaptación) y personalizar por composición (dos personas con misma estatura pero 50 vs 85 kg ya NO reciben la misma rutina).

**🆓 Modo libre completo (commits c0bd44f, 229a813, ffaad0e, 73faee8, d102d44):** auto-registro público (`signupClient`) con rutina auto-generada al instante; invitación a Premium + `requestCoach` que LE LLEGA al coach (mensaje + poll de `ax_c`); gating Premium (`isFreeClient` + `premiumLockHTML`); `convertToPremium`; editar libre ofrece regenerar. Ver sección "Auto-registro y modo libre". **NOTA:** sigue escribiendo en `ax_c` compartido (RLS permisiva) — aislamiento multi-trainer real = v2.0.

**Tests:** 72 → **111/111**. Caché `apex-v55`. Pendiente verificación visual en navegador (bloqueada por RAM/pagefile de la PC de Camilo — ver memoria). Idea Premium aprobada (parked): valoración inicial completa del principiante.

**🔐 Seguridad — evaluación + plan Auth+RLS APROBADO (👉 ARRANCAR AQUÍ la próxima sesión):**
- **Hallazgo crítico:** `apex_data` y `push_subscriptions` tienen políticas RLS `USING(true)` → la key `anon` (en el `index.html` público) da **acceso TOTAL** a toda la base (confirmado por `get_advisors(security)`). El "login" es client-side; passwords SHA-256. **No se puede compartir el enlace a usuarios reales** sin esto. Consejo vigente: mantener AVI en **círculo cerrado** hasta tener auth.
- **Plan APROBADO** (archivo: `~/.claude/plans/quiet-hatching-teapot.md`; memoria: `project_avi_auth_rls_plan`). Decisiones del PO: **puente pragmático** (tabla `user_data` con fila por usuario, RLS `auth.uid()=user_id OR =coach_id`), **un coach (Camilo) + libres** ahora (multi-trainer después, dejar `coach_id` listo), usuarios actuales **re-crean clave vía enlace** (SHA-256 no migra). Usar **supabase-js por CDN**.
- **Fase 0 HECHA (2026-06-02):** eliminada tabla `apex_coaches` (vacía/sin uso) → -2 alertas; `apex_data_backups` verificada (RLS sin política = bloqueada, OK). Diferidos: mover `pg_net` (no expone datos; riesgo cron) y privatizar bucket `apex-photos` (solo 2 fotos; necesita código de URLs firmadas → hacerlo con la auth).
- **Próximo: Fase 1** — requiere clics de Camilo: habilitar Supabase Auth (Email+Google) + credenciales OAuth en Google Cloud (guía: `docs/setup-login-google.md`); luego cargar supabase-js + crear `user_data`+RLS. Fases 2-4: reescribir capa de datos a fila-por-usuario, migrar, quitar políticas `USING(true)`.

### ✅ v2.0 — Auth real + RLS (Junio 2026) — rama `feat/auth-rls`, cutover en curso
**El cambio más grande del proyecto.** AVI pasa de "todo en blobs globales públicos (`apex_data`, RLS `USING(true)`)" a **login real con Supabase Auth + datos aislados por usuario (RLS)**. Convive con el sistema legacy durante la transición (rollout gradual).

**Arquitectura nueva (coexiste con la legacy):**
- **supabase-js** por CDN (`<script defer>` en `<head>`) + wrapper inline **`AUTH`** (init lazy `sbAuthClient()`; signUpEmail/signInEmail/signInGoogle/sendMagicLink/resetPassword/getSession/onChange/signOut). El boot offline-first NO depende del CDN.
- Tabla **`user_data`** (Supabase): 1 fila por usuario — `user_id` (PK→auth.users), `coach_id`, `role` ('coach'|'client'), `profile/routines/history/prs/bodyweight/medidas/nutrition/photos/msgs` jsonb. **RLS por operación:** SELECT/UPDATE = `auth.uid()=user_id OR =coach_id`; INSERT = solo `user_id`; DELETE = solo propia.
- Capa **`UD`** (inline): `loadOwn/upsertOwn/createFromClient/loadCoachClients/updateClientRow` (RLS con el JWT).
- Helpers puros en `avi-core.js`: **`clientToRow`/`rowToClient`** (mapeo cliente↔fila; la contraseña NO viaja, la maneja Auth) + `USER_DATA_COLLECTIONS`. +7 tests de mapeo → **118 tests** (la lógica de UD/auth/coach es de runtime, no unit-test).
- **`AUTH_MODE`** (flag): true cuando se entró por Auth. En modo auth, `sbSet` es no-op (no toca el blob global legacy) y `sv`/`svNow` enrutan a `user_data` (cliente→su fila vía `_persistAuthUser`; coach→fila del cliente que cambió vía `_persistCoachWrite` con diff `_coachSnap`). `AUTH_ROLE` distingue cliente/coach. **`pollMessages` en modo auth enruta a `_pollAuthData`** (cada 15s + al volver a foreground/online): `_pollAuthClient` re-trae la fila propia (`UD.loadOwn`) y refresca chat + plan EN VIVO sin salir/entrar — con guarda que no pisa edición en curso (`m-routine` abierto / `todayWorking` / `_authDirty`); `_pollAuthCoach` refresca bandeja, asesorados nuevos y `wantsCoach` (mensajes aditivos). Fix avi-v199 (2026-06-25).
- **`doLogin` auth-primero con respaldo legacy:** intenta `AUTH.signInEmail`; si la cuenta no existe en Auth, cae al login legacy intacto (coach `coach@apex.com` + clientes con SHA-256 en `ax_c`). Boot entra en modo auth si hay sesión Supabase. Helpers: `_enterAuthSession`/`_enterCoachAuth`/`_provisionFreeClient`/`_applyAuthClientDB`/`_profileFromMeta`.
- Registro libre (`signupClient`) → `AUTH.signUpEmail` (perfil en metadata) → provisiona rutina + fila. **Confirmación de correo DESACTIVADA** (temporal, para pruebas; reactivar con SMTP propio antes del público).

**Identidades:** coach real = `camilo06197@gmail.com` (role coach). Los **7 asesorados reales migrados** del blob global → `auth.users`+`auth.identities`+fila `user_data` (coach_id=Camilo), emails placeholder `@apex.com`, claves temporales (Camilo las reparte). `apex_data` se conserva INTACTO como respaldo.

**Crear usuarios por SQL (sin service_role):** insert en `auth.users` (instance_id `0…`, aud/role 'authenticated', `encrypted_password=crypt(pass,gen_salt('bf'))`, raw_app/user_meta_data) **+ `auth.identities`** (provider 'email', provider_id=uid::text) **+ columnas de token a `''` NO NULL** (confirmation_token/recovery_token/email_change*/phone_change*/reauthentication_token — su NULL ROMPE el login). Confirmar cuenta = `email_confirmed_at=now()`.

**Estado del cutover (2026-06-03): ✅ PUBLICADO a producción** (main, GitHub Pages, **SW apex-v63**). Auth real corriendo con respaldo legacy (auth-primero → si la cuenta no existe en Auth, cae al login viejo; nadie se bloquea). Verificado en celular: login viejo y nuevo conviven.

**Migración:** **6 asesorados reales** (Kathe, Samuel, Miguel, Natalia, Astrid, Nataly) en `auth.users`+`user_data` (coach_id = Camilo `camilo06197@gmail.com`). **OJO:** `andres@apex.com` ("Andres Martínez") **era Camilo** → su data se copió a la fila del coach (su "Mi entrenamiento") y la cuenta se ELIMINÓ. `apex_data` legacy INTACTO como respaldo.

**Features desplegadas post-cutover (apex-v56→v63):** quitado logo viejo "A" (solo wordmark AVI) · verdes unificados a esmeralda (`--g/--g2/--gl/--gt`=esmeralda oscuro, acento `#10E0A0`) · fix botón ☰ invisible en claro · 👁 ver-contraseña (login/registro/coach, `togglePass`) · **Login con Google** (`loginWithGoogle`/`AUTH.signInGoogle`) en login y registro · **Conectar Google** a cuenta existente (`linkGoogle`/`linkIdentity`; **"Manual linking" YA habilitado** vía Management API; VERIFICADO sin pérdida de datos) · fix UX: signups por Google preguntan perfil (`showProfileSetup`/`needsProfile`) antes de generar · **"Mi entrenamiento" del coach** (`openMyTraining`/`backToCoachPanel`/`COACH_SELF`; botón "← Panel" en la cntopbar).

**✅ FASE 4 COMPLETADA (2026-06-03, sesión 2) — candados cerrados:**
- **RLS:** eliminadas las 4 políticas `USING(true)` de `apex_data` (`apex_access`, `apex_open`) y `push_subscriptions` (`apex_access`, `push_access`). `apex_data`/`apex_data_backups` quedan con RLS ON sin política = **bloqueadas a anon** (el blob legacy se conserva INTACTO como respaldo, solo deja de ser legible). **La fuga crítica (anon leía toda la DB) está CERRADA.**
- **push_subscriptions:** NO se cerró del todo — `subscribePush` hace upsert con la key anon, cerrarla rompería el registro de dispositivos nuevos. Quedó con política **solo-escritura** (`push_write_insert` + `push_write_update`, sin SELECT/DELETE) → anon ya no puede enumerar tokens/clientes, pero sigue registrando. Envío de push sigue por service_role (Edge Functions). Advisor lo marca WARN (permissive write) — **tradeoff aceptado**; follow-up: mover push a cliente autenticado (RLS `client_id=auth.uid()`).
- **doLogin:** respaldo legacy ELIMINADO (líneas ~2533-2598). Login = SOLO Supabase Auth. Si supabase-js (CDN) no cargó → mensaje "No se pudo conectar" (no "contraseña incorrecta"). `verifyClientPass`/`verifyCoachPass`/`getCoachEmail` siguen definidas (las usa ⚙️ Configuración del coach) pero ya no se llaman en login.
- **Migración de datos blindada antes de cerrar:** se verificó cliente por cliente que `user_data` ≥ legacy (Miguel/Samuel tenían MÁS en auth; Astrid idéntica; Camilo conserva las 6 sesiones de "Andres Martínez"). **Nataly reconciliada** (su sesión 03-jun se guardó parcial 15:36 en user_data y final 17:38 en legacy → `mergeHistory`/`mergePRs` = versión legacy, superconjunto; fijada en su fila). Cero pérdida.
- **Astrid y Nataly** (no habían entrado al sistema nuevo): claves Auth reseteadas a valores conocidos — **Astrid → `Astrid2026`**, **Nataly → `Nataly2026`** (su email `@apex.com` es placeholder sin bandeja, así que el reset por enlace no les llega; Camilo les pasa la clave). ⚠️ Su PWA sigue en sesión legacy (auto-login desde caché) hasta que **cierren sesión y entren por Auth** — mientras tanto NO sincroniza. Camilo debe avisarles.
- **Cuentas de prueba BORRADAS** (auth.users + identities + user_data): `valery@avi.com`, `nuevo@avi.com`, `andres@avi.com`, `pavedwings38@gmail.com`, `stevang1204@gmail.com`. Quedan solo 7 reales: Camilo (coach) + 6 asesorados.
- **`COACH_UID` de Camilo (coach real) = `0a6484ed-42af-449d-9903-e440ac683ecf`** (útil para el cabo de `requestCoach`).
- Advisor final: crítico cerrado. Quedan WARN menores: 2× push write (tradeoff), `pg_net` en public (diferido), leaked-password-protection off (toggle Auth). INFO: apex_data/backups RLS sin política (= deseado).

**👉 RETOMAR MAÑANA (2026-06-04):** el plan Auth+RLS quedó TERMINADO; no hay nada urgente de código. Orden sugerido al arrancar:
1. **Chequear si Astrid y Nataly ya entraron** al sistema nuevo: `select email,last_sign_in_at from auth.users where email in ('astrid@apex.com','nataly@apex.com')`. Si su `last_sign_in_at` sigue null, recordarle a Camilo que les pase la clave (`Astrid2026`/`Nataly2026`) y que cierren sesión + entren por Auth (su PWA sigue en legacy sin sincronizar hasta entonces).
2. **Camino "abrir al público"** ✅ **HECHO (2026-06-04):** SMTP Brevo conectado + "Confirm email" ACTIVADO + plantillas en español. Registro público DESBLOQUEADO (ver sesión 2026-06-04 abajo).
3. **Camino "pulir auth"** (cabos menores, sin bloquear): mover push a cliente autenticado (cierra los 2 WARN de `push_subscriptions`); `requestCoach` que setee `coach_id` (ya hay `COACH_UID=0a6484ed-…`); chat en vivo (realtime/poll sobre user_data) en modo auth; ajustes nivel-coach (`ax_e`/`ax_tpl`) aún globales; cola de reintento de upserts; CSS muerto `.apex-logo`.

Plan completo: `~/.claude/plans/quiet-hatching-teapot.md` · memoria: `project_avi_auth_rls_plan`.

### ✅ Sesión 2026-06-04 — SMTP propio + Confirm email + plantillas en español (registro público DESBLOQUEADO)

**Lo único que faltaba del plan Auth+RLS para abrir al público quedó hecho.** Todo es **configuración de Supabase Auth** (server-side, vía Management API con PAT temporal ya revocado) — **NO hubo cambios de código** en el repo; `index.html` ya manejaba el flujo de confirmación.

**SMTP — proveedor Brevo** (gratis, no requiere dominio; verificación de remitente único):
- Config en Supabase auth (`PATCH /v1/projects/eoebhrxbokyllqalyecj/config/auth`): `smtp_host=smtp-relay.brevo.com`, `smtp_port="587"` (⚠️ **STRING**, no number — la API rechaza el número con "Expected string"), `smtp_user=ad8dd7001@smtp-brevo.com`, `smtp_admin_email=aviapptraining2020@gmail.com` (remitente del rebrand AVI, **verificado** en Brevo), `smtp_sender_name=AVI`, `rate_limit_email_sent=30`/h (era 2 — el SMTP de prueba con 2/h era el bloqueador original).
- ⚠️ **Un PATCH parcial de campos `smtp_*` BORRA el bloque SMTP completo** → reenviar SIEMPRE todo el bloque (incluido `smtp_pass`).
- Tropezones resueltos (en orden): (1) Brevo bloqueaba por **"IPs autorizadas"** (error SMTP `525 5.7.1 Unauthorized IP address`) → Camilo apagó el filtro en `app.brevo.com/security/authorised_ips` ("Desactivar para claves SMTP"). (2) **Remitente no verificado** (error Brevo "sender is not valid") → verificar el sender en `app.brevo.com/senders/list` (clic en el enlace que Brevo manda a esa bandeja). **NO era DMARC** (gmail.com usa `p=none`, sí entrega una vez verificado el remitente).
- Verificado **end-to-end**: el magic link de Supabase llegó a `aviapptraining2020@gmail.com`.

**Confirm email ACTIVADO** (`mailer_autoconfirm=false`). La app ya lo soporta: `signupClient` (index.html ~L3934-3941) — si `signUp` no devuelve sesión, muestra toast "📧 Te enviamos un correo para confirmar tu cuenta" y provisiona la rutina en el 1er login. **Sin cambio de código.**

**Plantillas de correo en español + branding AVI** (4: confirmación, magic link, recuperar contraseña, cambio de correo) vía `mailer_subjects_*` + `mailer_templates_*_content`. Diseño: logo **AVI** + lema oficial **"Entrenamiento con nombre propio"** debajo + botón esmeralda `#10E0A0` + cierre cálido **"Tu entrenamiento, contigo. 💚"** (combinación elegida por el PO).

**Nota:** los 6 asesorados con email `@apex.com` (placeholder falso) ya están confirmados → el confirm-email nuevo NO les afecta, pero NO pueden recibir reset por correo (Camilo gestiona sus claves a mano). **Técnica útil documentada:** capturar la pantalla de Camilo con `powershell.exe` + `System.Drawing.CopyFromScreen` (vía Bash→powershell.exe; el tool PowerShell directo dio "not available") para leer su panel de Brevo en vivo.

### 🔍 Auditoría profunda pre-lanzamiento (2026-06-04) — VEREDICTO: 🟢 APTA para abrir a más personas
Área por área, sin huecos que bloqueen el lanzamiento. (1) **Estático:** 7/7 checks + 118/118 tests. (2) **Backend:** 7 usuarios bien formados, 6 clientes→Camilo, 0 fugas de password en jsonb, 0 huérfanos, respaldo legacy intacto. (3) **Infra:** cron 7/10/17h + backup semanal activos, CORS de las 2 Edge Functions restringido a kronos-apex.github.io; **borrada la Edge Function huérfana `dynamic-responder`** (ejemplo "Hello {name}" de Supabase que quedó en prod). (4) **Seguridad código:** XSS cubierto (grep de interpolación de campos de usuario sin `esc()` = vacío; chat/nombre/notas pasan por `esc`), 0 secretos hardcodeados (solo key anon, acotada por RLS). (5) **Release:** SW apex-v64 y footer v2.0 sincronizados, sin deuda TODO/FIXME real. (6) **Auth/RLS:** aislamiento por usuario verificado, confirm-email enforced, 0 alertas de rendimiento.

**📋 AGENDADO — fix push a autenticado (cierra 2 WARN `rls_policy_always_true` de `push_subscriptions`).** Hoy `push_subscriptions` es escribible por anon (INSERT/UPDATE con `true`) → anon puede insertar/sobrescribir (no leer/borrar); ya acumuló **36 filas** para 7 usuarios (suscripciones viejas). **Alcance del fix:** (a) `subscribePush(clientId)` en index.html hace upsert con key anon → cambiarlo a usar el cliente Supabase autenticado (JWT de la sesión `AUTH`); (b) migración: añadir columna dueño ligada a `auth.uid()` + reemplazar políticas `push_write_insert`/`push_write_update` por RLS `client_id = auth.uid()` (INSERT/UPDATE/SELECT/DELETE propias); (c) limpiar las 36 filas viejas; (d) `send-push` (service_role) NO se afecta. **Riesgo:** no romper el registro de push de dispositivos existentes durante el cambio (hoy la tabla se keyea por clientId legacy, no por uid) → migrar/mapear con cuidado. Relacionado: cabos menores abajo.

### ✅ Sesión 2026-06-05 — Fotos de marca AVI en el wizard (DESPLEGADO el 2026-06-07, apex-v65)
**Contexto:** el wizard de onboarding premium (7 pasos, modo libre, módulo `WZ`, IDs `su-*` ocultos) ya estaba implementado en `index.html` pero **sin commitear** desde una sesión previa (+420/−61 líneas). Esta sesión añadió **fotografía de marca** a las pantallas grandes.

**Hecho:**
- Camilo generó 12 imágenes (ChatGPT/Gemini) en `Desktop/IMAGENES AVI`. Elegidas 4 (las de Gemini, las que clavan el ADN Noir Esmeralda). Las de torso desnudo/tono cálido → para **redes**, no para el wizard.
- Grade Noir Esmeralda + optimización con **ffmpeg** → `media/brand/`: `hero.jpg` (iij4u, trae logo AVI dorado), `reveal.jpg` (m0wvpb, de pie+glow), `ob-1.jpg` (5kovd, manos/barra, recorte 2:1), `ob-3.jpg` (o3hkpa, sentado+neón, recorte 2:1). **316 KB las 4** (fuentes pesaban ~28 MB; 1536×2752 c/u).
  - Grade usado: `eq=contrast=1.07:saturation=0.95,curves=master='0/0 0.25/0.19 0.5/0.5 0.85/0.88 1/1',colorbalance=bs=0.05:gs=0.02:bm=-0.02,noise=alls=4:allf=t+u` (versión `_GOLD` más suave para el hero, para no apagar el oro).
- **4 ediciones quirúrgicas en `index.html`** usando los hooks que ya existían: (1) `<video class="cin-vid">` ahora con `poster="media/brand/hero.jpg"`; (2) `showPlanReveal` cae por defecto a `media/brand/reveal.jpg` (`window.AVI_REVEAL_PHOTO` sigue como override); (3) ob-photo slide 1 → `has-img` + `--img:url('media/brand/ob-1.jpg')`; (4) ob-photo slide 3 → `has-img` + `--img:url('media/brand/ob-3.jpg')`. **Slide 2 ("Registra tu peso") se dejó con su ícono dorado a propósito** (variedad + acento oro).
- Verificado visualmente: los 4 assets + composite del Reveal con texto encima (legibilidad perfecta sobre el espacio negativo superior).

**Pendiente al retomar:**
- [ ] **Commitear** `media/brand/*` + `index.html` (sugerido: `feat(brand): fotos AVI en hero/reveal/onboarding`). OJO: el commit arrastra también el **wizard de 7 pasos** sin commitear de la sesión previa.
- [ ] **Captura real en navegador** quedó BLOQUEADA por RAM (322 procesos de Chrome, ~108 MB libres; el agente NO tocó el Chrome del usuario). Re-correr cuando libere memoria; mock fiel en `_preview-fotos.html` (untracked, **NO commitear**).
- [ ] QA Lucas (funcional) + Julián (estático) antes de deploy.
- [ ] **SW**: cache-first runtime sin lista de precache → las imágenes se cachean solas; solo **bump `apex-v64`→`apex-v65`** al deployar.
- [ ] Opcional: limpiar la marca **✦** del generador (esquina inf-der de las 4 fotos; el scrim casi la tapa).
- [ ] Footer de versión visible sigue en `v1.3.1 · May 2026` (pendiente histórico).
- Untracked que NO entran al commit de fotos: `_preview-fotos.html`, `scripts/demo/`.

### ✅ Sesión 2026-06-07 — Mega-sesión: wizard+fotos a prod, fix Google, rediseño interior dark, BUG coach, 109 fichas premium (apex-v65 → v79)
> ⚠️ **Nota de ruta:** la carpeta del repo en el Desktop se renombró `apex` → **`AVI`**. Ruta local ahora `C:\Users\KRONOS\Desktop\AVI\apex-app`.

**Todo desplegado a producción.** Lo de la sesión 06-05 (wizard 7 pasos + fotos de marca) por fin se commiteó/desplegó, más mucho trabajo nuevo:

1. **Wizard + fotos de marca DESPLEGADOS** (apex-v65, `4929a0f`): onboarding premium (módulo `WZ`, reveal del plan, 17 íconos SVG propios, 4 fotos `media/brand/`) a producción.
2. **🐛 Fix registro con Google** (apex-v66, `86f5039`): el paso 7 con Google redirigía y perdía las respuestas del wizard → caía al formulario viejo. Fix: `wzGoogle()` guarda los `su-*` en `localStorage['ax_wz_pending']` antes de redirigir; `_pendingWizard()`+`_profileFromMeta` los recuperan al volver → genera semana → reveal nuevo (caduca 30 min).
3. **📸 Primeras 10 fotos de ejercicio AVI** (apex-v67, `f645441`): Gemini (modelo propio ♂/♀, físico natural) + grade Noir Esmeralda + recorte 720² + delogo ✦ → `media/exercises/`. Reasignación: e85 (aperturas polea ALTA) ≠ e3; e71 cambiada luego a variante de mancuernas inequívoca.
4. **🎨 Rediseño interior DARK** (apex-v68→v71, +v79). **Causa raíz: el interior estaba en tema CLARO por defecto** (solo oscuro si el celular lo estaba) → chocaba con el onboarding oscuro. (a) tema dark Noir por DEFECTO (`initTheme` `'auto'`→`'dark'`); (b) hero "Hoy" Aurora + eyebrow Anton dorado; (c) foto del ejercicio en cada tarjeta (`.cex-thumb`); (d) detalle de ejercicio con foto full-bleed + scrim; (e) callouts/tintes coherentes en dark (override `--yll/--bll/--orl/--rdl` + browns hardcodeados → vars). **Lección:** texto sobre las fotos de ejercicio (siempre oscuras) va blanco fijo, no `var(--t1)` (fix modo claro del título, v79).
5. **🔴→✅ BUG CRÍTICO: solicitudes de coach invisibles** (apex-v72/v73, `57c456d`/`fc06484`). Un usuario libre se creaba con `coach_id=NULL` → el coach (RLS select `coach_id=auth.uid()`) NUNCA veía la solicitud ni los mensajes (lo de la sesión 06-02 "le llega vía ax_c compartido" dejó de servir al cerrar el RLS en Fase 4). **Lead real perdido: Cristhian Calderón, 6-jun.** Fix: `requestCoach()` y `_provisionFreeClient` asignan `coach_id=COACH_UID`. **Decisión de Camilo: TODOS los self-reg entran al pipeline del coach** (ve cada registro con tag 🆓 Libre / 🙋 Quiere coach). Backfill SQL de los existentes. `COACH_UID=0a6484ed-…ecf`.
6. **📝 LAS 109 FICHAS DE EJERCICIO a estándar premium** (apex-v74→v78): nombre específico + descripción que NOMBRA el músculo trabajado (cabeza/zona enfatizada) + criterio, en versión técnica (coach) y simple (asesorado). Patrón: reescribir solo las flacas, dejar intactas las ya buenas (mucho del catálogo ya estaba bien); cardio no lleva "músculo". 118/118 tests en cada deploy.

**Fotos de ejercicio — flujo DEFINITIVO (Camilo decidió):** **GEMINI hace TODO el look** (oscuro Noir + verde + logo AVI impreso en la tela — lo integra mejor que cualquier post). Claude **no** puede generar personas ni debe estampar el logo (se ve plano/artificial; Camilo lo rechazó). Claude SOLO quita la ✦ + resize 720². Guía completa en `Desktop/AVI/PROMPTS-EJERCICIOS-BIBLIOTECA.md` (Paso 1 = bloque+foto **una vez**; Paso 2 = **solo la línea** del ejercicio). Skills de IA para el logo (RunComfy) descartadas por ser de pago.

**Pendiente al retomar:**
- [ ] Generar las **~99 fotos de ejercicio restantes** en Gemini (con la guía nueva) → Claude las procesa (✦ + 720²) e integra por lotes a `media/exercises/`.
- [ ] Fotos correctas aún faltantes: **e27** (jalón agarre neutro — Gemini no dibuja el maneral), **e86** (aperturas polea baja), **e3** (aperturas con cable genérica).
- [ ] Opcional: nav inferior de emojis (`.ctico`) → íconos SVG propios.

### ✅ Sesión 2026-06-07 (cont.) — Fotos lote 2, fix mapeo, Nota del coach (109/109), descripciones (apex-v80 → v84)
> Ruta del repo: `C:\Users\KRONOS\Desktop\AVI\apex-app`. Carpeta de fotos que dejó Camilo esta sesión: **`Desktop/IMAGENES EJERCICIO AVI`** (distinta a la documentada `fotos-nuevas`).

1. **📸 23 fotos de ejercicio — lote 2** (apex-v80, `fa5fb06`): 22 reemplazos (e4,e5,e8,e9,e11,e14,e16,e19,e21,e22,e27,e28,e30,e38,e53,e54,e55,e57,e79,e84,e98,e99) + **e97 Pike Push-up**. ⭐ e97 YA EXISTÍA en el catálogo pero **faltaba en `EX_IMG_NAME`** → sin esa entrada `exImgSrc` no resuelve la foto aunque exista el archivo. **e27 (jalón neutro) ya quedó con foto** (sale de la lista de pendientes).
2. **🔧 Fix e9 + recortes que comían la técnica** (apex-v81, `ef16be0`): `e9` mostraba MANCUERNAS en vez de barra — el archivo fuente venía **mal nombrado por Gemini**; corregido con la imagen de barra real. Las 4 fuentes **verticales** (e14,e16,e21,e38) se recortaban al cuadrado y perdían pies/manos (a un calf raise le comía los talones). **`scripts/process-photos.sh` endurecido:** detecta orientación y, si NO es cuadrada, hace **fit completo + relleno de fondo desenfocado** (no recorta el sujeto). **Lección (memoria `feedback-avi-imagenes-verificar-contenido`): MIRAR el contenido de cada imagen, no confiar en el nombre del archivo.**
3. **⭐ NUEVA FEATURE — "Nota del coach" por ejercicio** (apex-v82 lote 1 `fc5761f` → apex-v83 completo `a338303`): callout esmeralda en el detalle del ejercicio, **visible para el asesorado**, con un cue profesional corto por movimiento (técnica/excéntrico/seguridad). Mecánica = mapa estático **`EX_COACHTIP`** (junto a `EX_IMG_NAME`), resuelto por id al renderizar (`ex.coachTip || EX_COACHTIP[id]` en `_showExSheet`) — **sin migración ni sync, retro-compatible** (mismo patrón que las imágenes). UI: sección `#exd-coachtip-wrap` + CSS `.exdetail-coachtip`/`.ctip-*`. **109/109 ejercicios cubiertos.**
4. **📝 22 descripciones enriquecidas** (apex-v84, `510805b`): mejoradas las `descSimple` más cortas (<140c) con setup + cue de error/seguridad. **Criterio honesto:** 87 de 109 ya eran sólidas → NO reescritura ciega (sería churn arriesgado); se mejoró solo lo flaco.

**Pendiente al retomar:**
- [ ] Seguir generando las **fotos de ejercicio restantes** en Gemini (33/109 con foto real). Aún sin foto correcta: **e86** (aperturas polea baja), **e3** (aperturas con cable genérica). Camilo sigue revisando → pueden salir más mismatches de contenido.
- [ ] Opcional: reforzar descripciones puntuales que Camilo marque (las 22 cortas ya están; las 87 buenas se dejaron).

### ✅ Sesión 2026-06-08 — Onboarding full-bleed + fotos AVI con logo + limpieza Nota del coach (apex-v85)
> Ruta del repo: `C:\Users\KRONOS\Desktop\AVI\apex-app`.

1. **🧹 Eliminada la "Nota del coach" DUPLICADA en el detalle de ejercicio** (`510805b`…): el detalle mostraba DOS cajas de nota — la nueva esmeralda `EX_COACHTIP` ("Nota del coach", visible al asesorado) **y** la vieja gris "Notas técnicas (coach)" (`#exd-tech-wrap`, mostraba `ex.desc`, solo-coach). Camilo pidió dejar solo la esmeralda. Quitado el bloque HTML `#exd-tech-wrap` + su JS en `_showExSheet`. El coach ya no ve la descripción técnica larga en el detalle (queda el cue esmeralda). `ex.desc` sigue como dato y como fallback de `descSimple`.
2. **🎨 Onboarding intro (`#onboarding`, primer uso) rediseñado a FULL-BLEED inmersivo.** Causa: las fotos vivían en una cajita de 188px (tirita) → poco impacto. Ahora cada slide = **foto de marca a pantalla completa** (cover) + degradado inferior + texto (eyebrow/título 30px/cuerpo) anclado abajo en `.ob-copy` + dots y botones flotando encima. Mecánica del slider intacta (`translateX`, `obGoTo/obNext`). Slide 2 pasó de ícono dorado a foto.
3. **📸 3 fotos de marca AVI con logo en la camiseta** (`media/brand/ob-1/2/3.jpg`), **1536×2752 nativo**, ~220–248 KB c/u, grade Noir Esmeralda + ✦ eliminado (delogo). Flujo: re-cortadas de los originales verticales Gemini (1536×2752, mucho mejor que el recorte 2:1 viejo) → **Camilo regeneró en Gemini con el logo AVI horneado en la tela** (NO estampado por Claude — sigue la regla [[feedback-avi-imagenes-verificar-contenido]] de que Gemini hace el logo) → Claude solo delogo+grade+nativo. ob-1=peso muerto (13uqux, AVI arriba), ob-2=de pie (euuvec, AVI grande), ob-3=sentado (aehg39, AVI). Originales con logo archivados en `Desktop/IMAGENES AVI/AVI_logo_*.png`. Descartadas: pose "modo bestia" (xt5odg) y la de barra "plasma" sci-fi (8jovz2) por romper el tono premium / vibe Gravl.

**Pendiente al retomar:**
- [ ] Aplicar el estilo full-bleed a más **momentos** (NO pantallas de uso diario, se diluye): top picks = **Fin de entrenamiento 🏆** + **upsell Premium/"Quiero un coach" ⭐** + descanso entre series + subir de nivel. Idea aprobada en concepto; falta implementar.
- [ ] Variedad de sujetos: meter **atleta mujer** en esas pantallas (Camilo genera en Gemini, Claude procesa).
- [ ] Footer de versión visible sigue en `v1.3.1 · May 2026` (histórico).

### ✅ Sesión 2026-06-08 (cont.) — Fin de entrenamiento full-bleed 🏆 (apex-v86)
**Primer "momento" full-bleed fuera del onboarding** (ver principio: el estilo se reserva para picos emocionales, no uso diario). Overlay `#workout-finish` (z-900) que salta al completar el **100%** de una rutina: foto de marca a pantalla completa + scrim + 🏆 + "¡Lo lograste, {nombre}!" + chips de resumen (**series · volumen · récords**) + PRs nuevos (máx 3 + "+N más") + confetti + botón "Continuar".
- **Disparo:** `showWorkoutFinish(routine,{done,total,totalVol,newPRs})` reemplaza al banner inline `#congrats` en los DOS puntos de completado: `updateClientProgress` (marcado normal) y `checkAndShowCongrats` (modo guiado). Guard `_wfShownFor=routineId|día` evita re-pop al re-marcar la última serie.
- **Foto:** `WF_DEFAULT_PHOTO='media/brand/ob-2.jpg'` (de momento reusa la de pie del onboarding) con override `window.AVI_FINISH_PHOTO`. **Pendiente:** foto de VICTORIA dedicada (Gemini, atleta triunfante NO posando, camiseta AVI) → swap de 1 línea por escasez/variedad.
- **Dead code inerte (a propósito, sin riesgo):** `#congrats` (banner viejo) + `showPRCelebration` quedan definidos pero ya no se muestran; las llamadas `congrats.classList.add('hide')` son no-ops.
- 118/118 tests + audit 8/8.

### ✅ Sesión 2026-06-08 (cont. 2) — Cierre con datos + "¿Cómo te sentiste?" para el coach (apex-v87)
Sobre la pantalla "Fin de entrenamiento" (benchmark: captura real de Gravl "Detalles del entrenamiento" en `Downloads/_avi_review_extract/` — muestra Fecha, Duración, Energía/Kcal, Volumen, Récords, Músculos, Ejercicios + estrellas "¿Cómo fue tu entrenamiento?").
1. **Datos nuevos en el cierre:** ⏱️ **Duración** (desde la 1ª serie, `startedAt` en la entrada de historial) + 🔥 **Calorías aprox.** (MET 5.5 × peso × horas; fallback 70 kg) + 📅 **Fecha** (locale es-CO). Se persisten en la sesión (`startedAt`/`durationSec`/`kcal`). Chips 2×2: Duración · Calorías · Series · Volumen. Helper `fmtDuration`.
2. **⭐ FOSO vs Gravl — "¿Cómo te sentiste hoy?"** 5 caritas (😫😕😐🙂😄 = `feeling` 1-5) en el cierre → se guarda en la sesión + sincroniza. **El COACH la ve** en el historial del asesorado (`renderCoachClientHistory`: carita + duración + kcal por sesión). Gravl pide la calificación pero su coach es IA; AVI la usa un humano real. `WF_FEELINGS`/`feelingEmoji`/`feelingLabel`/`wfRate`/`_wfEntry`. Sin migración (retro-compatible).
3. Identidad mantenida (no copia de Gravl): foto full-bleed + "¡Lo lograste, {nombre}!" + esmeralda/oro + 🏆 + confetti, vs la tarjeta plana fría de Gravl.
- 118/118 tests + audit 8/8.

### ✅ Sesión 2026-06-08 (cont. 3) — Fotos de ejercicio GENDER-AWARE, Fase 1 (apex-v88)
**Feature nueva:** la asesarada ve la demo del ejercicio en MUJER; coach/hombre ven el default. Mecánica en `exImgSrc(e,sex)`: si `sex==='F'` y el id está en `EX_IMG_F`, devuelve `media/exercises/{id}_f.jpg`; si no, el `{id}.jpg` de siempre. `sex` por defecto = `_viewSex()` (en vista del asesorado sigue su `client.sex`; coach → default). **Sin tocar los ~8 call-sites** (exIcon/exImgSrc heredan el default). Retro-compatible y aditivo.
- **Fase 1:** 3 fotos de mujer procesadas (delogo ✦ + 720²) → `e2_f.jpg` (press inclinado), `e26_f.jpg` (jalón amplio), `e50_f.jpg` (buenos días). `EX_IMG_F={e2,e26,e50}`.
- **Origen:** Camilo dejó 43 fotos en `Desktop/AVI/fotos-seleccionadas/` (16 con nombre eN-...__variante + 27 `Gemini_*` de atleta MUJER sin mapear). Todas premium Noir + logo AVI.
- **PENDIENTE Fase 2:** (a) las **27 fotos de mujer sueltas** → necesitan ID de ejercicio (Camilo renombra como las 16, o Claude propone mapeo para que confirme) → procesar a `{id}_f.jpg` + ampliar `EX_IMG_F`. (b) **7 upgrades de hombre** seleccionados (e6,e11,e13,e71,e77,e83,e85; tienen variantes de ángulo) → reemplazar `{id}.jpg` si Camilo lo aprueba. 118/118 + audit 8/8.

### ✅ Sesión 2026-06-08 (cont. 4) — Gender-aware Fase 2: 21 fotos mujer + 4 hombre (apex-v89)
De las 43 fotos de `fotos-seleccionadas/`, procesadas las 27 `Gemini_*` (delogo ✦ + 720², tamaños mixtos cuadrado/apaisado/vertical manejados). **Claude identificó cada una a resolución full contra `DB.exercises`** (no miniaturas, no EX_IMG_NAME).
- **21 fotos de MUJER** → `{id}_f.jpg`: e96,e73,e38,e93,e40,e18,e58,e14,e41,e106,e35,e48,e37,e62,e42,e89,e13,e17,e80,e44,e92. `EX_IMG_F` ampliado a 24 ids (con los 3 de Fase 1: e2,e26,e50).
- **4 fotos de HOMBRE** (modelo masculino, no mis-sorted) → reemplazo de `{id}.jpg` default: e36 prensa, e4 dominadas, e47 rueda abdominal, e94 abducción de pie con banda (e94 era nueva).
- **e89, e93, e94 AÑADIDAS a `EX_IMG_NAME`** (existían en DB.exercises pero faltaban en el mapa → si no, la foto no resolvería; misma trampa que e97). Lección guardada en memoria [[feedback-verificar-catalogo-ejercicios]].
- Descartadas 2 fotos mujer duplicadas (2ª toma de e73 y de e44).
- 118/118 tests + audit 8/8.

**✅ 7 upgrades de HOMBRE aplicados (apex-v90):** reemplazado `{id}.jpg` de e6 (jalón espalda), e11 (extensión tríceps abajo), e13 (sentadilla barra), e71 (press mancuernas abajo), e77 (flexiones pared), e83 (lagartijas alt), e85 (aperturas polea cerrado). Multi-ángulo: elegido el frame más representativo por ejercicio. delogo + 720².
**Pendiente:** más fotos de mujer para cubrir más ejercicios (24/109 con versión mujer).

### ✅ Sesión 2026-06-12 — Catálogo visual COMPLETO + pipeline Gemini (apex-v143)
- **Pipeline de imágenes por API Gemini**: skill gemini-api + `Desktop/AVI/gen-img.mjs` (GEMINI_API_KEY en env de usuario, modelo gemini-3.1-flash-image ~$0.04/img, soporta `--ref` pose+persona). Receta: ref de POSE (foto validada del catálogo) + ref de PERSONA (e50_f mujer canon) + staging + revisión de Camilo ANTES de integrar.
- **108 fotos _f nuevas** (atleta canon) revisadas y aprobadas por Camilo → `EX_IMG_F` 27→136 ids. Rechazadas sin integrar: e21, e88, e131 (regenerables con modelo Pro).
- **e126 Curl Femoral Sentado**: fotos reales de Camilo (díptico inicio/flexión, app Gemini gratis) → **catálogo 138/138 con foto, cero stock**. Gemini por prompt NUNCA logró el curl femoral (siempre dibuja extensión de cuádriceps) — para máquinas confundibles usar foto real como ref.
- Pipeline de deploy se pagó solo: Lucas QA atrapó e126 fuera de `EX_IMG_IDS` (la foto no resolvía). Suite estática reutilizable en `Desktop/AVI/qa-julian.mjs`. Deploy 4ce9fc6.
- **Pendiente VIDEOS (próxima sesión):** 106 ejercicios sin video (lista en `Desktop/AVI/videos-faltantes.json`); decisión 2026-06-09 vigente = UN video por ejercicio para todos; plan = imagen→video desde la foto aprobada del catálogo; presupuesto API por definir vs flujo gratis de Camilo.
- **Pendiente PLAY STORE:** política de privacidad con URL pública + borrado de cuenta self-service + formulario Data Safety + cuenta dev (US$25). OJO: TWA/assetlinks.json (v1.1) y docs en `legal/` YA existen — revisar vigencia antes de rehacer.
- **AVI GYM**: idea de la dueña del gym → proyecto SaaS multi-gimnasio SEPARADO en `Desktop/AVI-GYM/avi-gym-app` (fork). **AVI personal queda exclusiva de Camilo**; mejoras fluyen solo AVI→GYM.

## Hitos por sesión (crudos, más reciente primero)

*Hitos sesión 2026-07-07 (parte 15 — BLINDAJE anti-errores para sesiones futuras, pedido explícito de Camilo, sin deploy de app): "no seas complaciente, sé crítico, bugs de raíz, cada feature premium, muéstrame lo que no veo". (1) CLAUDE.md: sección nueva **🛡️ DOCTRINA DE TRABAJO** al tope (Camilo es entrenador no dev → el agente es el ingeniero responsable; anti-complacencia como regla dura; protocolo de bugs de RAÍZ en 5 pasos con prohibiciones; barra PREMIUM = Definition of Done de 8 puntos; RADAR obligatorio al cierre de sesión — máx 5 puntos honestos de lo que Camilo no ve; documentación viva; guardrails duros). (2) Hook pre-commit: **check 11 anti-borrado de tests** — baseline versionado en scripts/hooks/suite-baseline; si la suite BAJA de conteo el commit se bloquea (el atajo clásico de un fix flojo es borrar el test que estorba); si sube, el baseline se auto-actualiza y se auto-agrega al commit. Probado: bloqueo con baseline inflado OK. (3) Skills actualizados: avi-deploy (gates mecánicos explícitos, PASO 0 bump del par de versión, PASO 6 verificación curl de Pages obligatoria, PASO 7 documentar en bitácora), avi-feature (pipeline de bug fix = protocolo de raíz con repro/causa/regresión/bitácora; reglas 6-8: barra premium, anti-complacencia, radar). Referencias 9/10 checks → 11 y footer/IDENTIDAD actualizados a v2.x.*

*Hitos sesión 2026-07-07 (parte 14 — pantalla de fin no salió a Claudia, avi-v293/v294): reporte Camilo "a Claudia no le apareció la pantalla al terminar" (a él sí). DIAGNÓSTICO con datos: su sesión de hoy quedó GUARDADA 19/19 en user_data (la rama done===total corrió) y app_errors NO registró nada de su equipo; prod servía v290 durante su sesión (v291 salió después). Los 7 caminos de finalización verificados SANOS con harness nuevo `scripts/e2e/_repro-wf-claudia.mjs` (en orden, fuera de orden, plancha/HIIT/cardio de últimas, guard anti re-pop, con ánimo). El modo de falla que calza: un throw ENTRE saveSessionToHistory y showWorkoutFinish (checkAndUpdatePRs / renderPRsInProfile / renderClientExProgress) mata la celebración en silencio CON el historial ya guardado. BLINDAJE v293: esos pasos en try/catch con reporte a app_errors (wf-prs / wf-refresh) y la pantalla sale igual; el guard _wfShownFor se fija AL FINAL (antes, un throw a mitad quemaba el día entero sin celebración). v294: toLocaleDateString('es-CO',{weekday...}) lanza RangeError en WebViews sin ICU completo (Huawei viejos) → fallback manual sin locale. Harness 9/9 (W8 = throw en PRs → celebra igual y reporta; W9 = guard no quemado → re-marcar reintenta). No se pudo determinar la causa EXACTA en el equipo de Claudia (sin error en telemetría): candidatos = build vieja cacheada por red lenta de gym (pre-v282, sin telemetría), throw de ICU, o cierre accidental con el gesto de atrás (cierra workout-finish de primero en _aviCloseTopOverlay). Desde v293 cualquier recurrencia deja huella en app_errors. PENDIENTE preguntar a Claudia: qué celular tiene y si vio ALGO al marcar la última serie.*

*Hitos sesión 2026-07-07 (parte 13 — CLAUDE.md adelgazado, tarea #6, sin deploy): 185KB → 34KB. Nace ESTE archivo (docs/bitacora.md) con el roadmap histórico completo y los 85 hitos crudos movidos desde CLAUDE.md; allá quedan solo el contexto vivo, la sección nueva "🧠 GOTCHAS VIGENTES" (destilado de lecciones que no expiran) y el ROADMAP con backlog vigente. REGLA NUEVA: los hitos de cada sesión se anotan AQUÍ (más reciente primero), no en CLAUDE.md; a CLAUDE.md solo van cambios del contexto vivo (arquitectura, gotchas, backlog, footer de versión).*

*Hitos sesión 2026-07-06 (parte 10 — F5b: LA CLÁSICA MURIÓ, avi-v291): borrado del código clásico huérfano tras F5a. Fuera: renderClientExList+updateBlockHeader (la lista completa), buildHiitCard/startHiit/stopHiit + HIIT clásico, HOLD/startHoldTimer/cancelHold/_endHoldUI (isométrico clásico), setLogInputsHTML/setDoneToast/setLogHeadHTML, buildWarmupSection/toggleExWarm, buildDropRow, el rest-banner COMPLETO (funciones + #rest-banner de index.html + restInt/_restVis/_restPaused) y ~70 líneas de CSS muerto (.cex-block/.set-log-row/.sinput/.scheck/.restbanner/...). renderClientToday: si el guiado embebido lanza → tarjeta de error "Recargar la app" (nunca pantalla en blanco). resetSession quedó limpio de DOM clásico (la limpieza de timers la hace gmResetSession); logout ahora para los timers del GUIADO (GM.restTimer/hiit/holding + banner mini). SE QUEDARON (verificado grep + suite + E2E): renderWarmup, todayMoveEx/todaySubstitute, finishSessionEarly/resetSession, attachDropSwipe + .setrow-wrap/.drop-reveal, pickMood/moodChooserHtml/moodBannerHtml, _suggestKg, updateClientProgress/updateVolSummary (null-guarded a los ids clásicos muertos), startRoutineNow, exSetsCellHTML, .wohero/.wopill (header del guiado), .cex-reorder (botonera reorden del guiado), checkDone. GOTCHA de la cirugía CSS: al quitar tokens de selectores multi-tema quedaron dos selectores PEGADOS (html[data-theme] duplicado) — corregido; revisar selectores compuestos tras ediciones por regex. ⚠️ `_repro-plancha.mjs` ROTO (usaba el flag y la clásica) — portar S5-S22 en tarea #19; cobertura interina: _verify-f5a/_verify-restmini/_verify-cardio/_repro-back-v243 + telemetría. Plan docs/plan-unificacion-guiado.md CERRADO (F0→F5 completas).*

*Hitos sesión 2026-07-07 (parte 11 — consentimiento Habeas Data conectado al registro, avi-v292): decisión Camilo 2026-07-06 "conecta los textos de consentimiento" (tal cual, SIN abogado aún — los docs de legal/ siguen marcados BORRADOR). Paso 7 del wizard: 3 casillas SEPARADAS y NO pre-marcadas (general = T&C + Política; salud = datos sensibles Ley 1581 explícito/voluntario/revocable; mayoría de 18). Gate en AMBOS caminos: signupClient (email) y wzGoogle (la evidencia viaja en ax_wz_pending porque OAuth saca de la página; caduca 30 min como el resto). Evidencia = "prueba de autorización" legal: `consentEvidence(checks, LEGAL_V, nowIso)` PURA en avi-core (3 tests, suite 277/277) → meta del signUp → `_profileFromMeta` → `rec.consent` en la fila user_data (clientToRow copia todo el perfil). `LEGAL_V='2026-07-07-borrador'` en app-3 — SUBIRLA cuando cambien los textos. Visor in-app `showLegalDoc('politica'|'terminos')` → modal `.mdbg#m-legal` (atrás de Android lo cierra gratis por la rama genérica de _aviCloseTopOverlay) con `_legalMdToHtml` (render mínimo, NO parser general). Docs de legal/ renombrados APEX→AVI (coherencia de marca en el visor; LEEME interno intacto). Harness `scripts/_verify-consent.mjs` 8/8 SIN tocar GoTrue (AUTH.signUpEmail y loginWithGoogle espiados → cero cuentas reales y cero rate limit). GOTCHA CDP: `await` de nivel superior en Runtime.evaluate es error de sintaxis SILENCIOSO (result undefined) — envolver en `(async()=>{...})()` con awaitPromise. Usuarios PRE-existentes no tienen consent (no se les bloquea nada; solo aplica a registros nuevos). Pendiente legal real: revisión de abogado + botones "descargar/eliminar mis datos" (LEEME §3).*

*Hitos sesión 2026-07-07 (parte 12 — suite E2E del guiado post-clásica, tarea #19, sin deploy): `scripts/e2e/_guiado-suite.mjs` **53/53 OK a la primera, jsErrors []** — puerto de S3-S22 de `_repro-plancha.mjs` (BORRADO del árbol; queda en git history) al mundo F5. Adaptaciones clave del puerto: setup = `renderClientToday` embebe (ya no setUiGuided+openGuidedMode); S5 gmFinishEarly NO oculta el embebido (es el tab); S9 volumen verificado en historial `totalVol` (el #prog-vol clásico murió); S15 atrás+descanso = MINIMIZA a banner (v288) y atrás+plancha = CANCELA (v245); S20b throw → tarjeta "Recargar la app" (ya no hay clásica de respaldo); S12/S16 (flag/interruptor) obsoletos, cubiertos por _verify-f5a. GOTCHA del puerto: el chooser de ánimo CONVIVE con las tarjetas del guiado — los escenarios NO deben elegir ánimo "para quitarlo de encima" porque pickMood ADAPTA la rutina (cambia sets/ejercicios) y rompe las expectativas; solo S6/S17 lo tocan a propósito. `openGuidedMode` (overlay) quedó SIN callers tras F5 — candidato a borrarse en una limpieza futura junto con la rama no-embebida de closeGuidedMode/_aviCloseTopOverlay.*

*Hitos sesión 2026-07-06 (parte 9 — F5a: retiro del FLAG de la vista guiada, avi-v290): Camilo validó guiado+descanso escondible+cardio en su celular y ordenó ejecutar F5 (waiveó el gate de 2 semanas del plan — ahora la telemetría app_errors vigila regresiones en prod). F5 partido en dos deploys: **F5a (este)** retira el flag `ax_ui_guided` y TODOS sus caminos: uiGuided/setUiGuided/_initUiGuidedFromUrl (app-1), llamada del boot (app-2), interruptor del Perfil renderGuidedViewToggle/switchTo*/_switchTodayView + #cn-guided-card (app-4 + index.html). El guiado embebido es la ÚNICA vista de "Hoy"; el opt-out viejo en localStorage se ignora. La lista clásica queda SOLO como paracaídas del try/catch de renderClientToday hasta F5b. Verificado: 274/274, smoke, `scripts/e2e/_verify-f5a.mjs` (login real: embebe sin flag ignorando opt-out, Perfil sin interruptor, funciones retiradas, jsErrors []) y regresión `_repro-back-v243` TODO OK. ⚠️ `_repro-plancha.mjs` llama setUiGuided (escenarios clásicos S1-S4/S12) → ROTO hasta la cirugía de harness de F5b. **F5b pendiente:** borrar código clásico huérfano (renderClientExList, buildHiitCard/startHiit/stopHiit clásicos, setLogInputsHTML, buildWarmupSection, startHoldTimer/HOLD, startClientRest/_stopRest/restPause/restAdd15/skipRest + #rest-banner, CSS .cex-*/.set-log-*), catch→tarjeta de error con Reintentar (ya sin clásica), harness reescrito. SE QUEDAN (los usa el guiado/coach): renderWarmup, todayMoveEx/todaySubstitute, finishSessionEarly/resetSession, attachDropSwipe, pickMood/moodChooserHtml/moodBannerHtml, _suggestKg, updateClientProgress, startRoutineNow, exSetsCellHTML.*

*Hitos sesión 2026-07-06 (parte 8 — CARDIO con cuenta regresiva, avi-v289): pedido de Camilo — en cardio (bici estática) poner p.ej. 20 min, dar ▶ Empezar y que un cronómetro AVISE al terminar. El guiado ya no pinta filas MIN+KM para cardio: tarjeta dedicada `gmRenderCardio` (patrón HIIT; inputs MINUTOS + KM opcional persisten vía setLog min/dist) + `gmCardioTimer` que reusa el overlay en modo en-curso (ámbar gm-hold + clase gm-cardio) con display mm:ss. Al llegar a 0: pitido + vibración + toast + marca todas las series del bloque y avanza (cierre gmAfterHiit). El cardio SÍ se minimiza al banner de v288 (largo; banner dice "cardio" con botón ⏹ cancelar) — la plancha sigue sin minimizarse (v245). ⏸ Pausar y +15s FUNCIONAN en cardio (pausa de agua): el crono usa GM.restEndAt/restTotal (no locales). Cancelar (⏹) no marca nada. GOTCHA cazado: el cableado del tap-aside vivía solo en gmShowRest → un cardio SIN descanso previo no minimizaba; ahora `_gmWireRestMinimize(overlay)` se llama desde ambos. Verificado scripts/_verify-cardio.mjs (8/8) + regresión _verify-restmini.mjs (8/8).*

*Hitos sesión 2026-07-06 (parte 7 — descanso del guiado MINIMIZABLE, avi-v288): pedido de Camilo tras probar el guiado en su celular — el descanso fullscreen gusta, pero debe poder esconderse para usar la app (ver el siguiente ejercicio, chat). Tocar A UN LADO (fuera de botones) o dar ATRÁS minimiza a banner flotante `#gm-rest-mini` (píldora verde fija abajo, creada por JS) con el conteo EN VIVO + botón "Saltar ⏭"; tocar el banner re-abre el fullscreen. El timer jamás se toca (mismo GM.restTimer). El banner lleva WATCHDOG (1s): si el descanso murió por cualquier ruta de teardown (cerrar guiado, reset), se retira solo — nunca queda huérfano. El ATRÁS ya NO salta el descanso (antes gmSkipRest): ahora minimiza; en isométrico (GM.holding) el atrás SÍ sigue cancelando el crono (v245, acción explícita) y el tap-aside no aplica (hint oculto vía .gm-hold). Hint en el overlay: "Toca a un lado para seguir usando la app". Funciones: gmMinimizeRest/gmExpandRest/_gmEnsureRestMini/_gmRemoveRestMini (app-6). Verificado con scripts/_verify-restmini.mjs (8/8: minimizar, conteo vivo, re-abrir, atrás, saltar, watchdog, hold).*

*Hitos sesión 2026-07-06 (parte 6 — racha SEMANAL, avi-v287): decisión de Camilo — la racha por días de calendario consecutivos castigaba al de 3/sem (vivía en "Empieza tu racha hoy"). Ahora racha = SEMANAS de calendario (Lunes→Domingo) seguidas cumpliendo la meta de días del plan. Núcleo puro en avi-core (+7 tests, 274/274): `planDays(client)` (rutinas con día ≠ Libre, fallback client.days, clamp 1–7), `weekStreak(sessions,target,now)` → {weeks,thisWeekDays,target,metThisWeek} (la semana EN CURSO no rompe la racha; si ya cumplió, la extiende), `longestWeekStreak` (récord histórico). UI: chip del Hoy ("🔥 N semanas cumpliendo tu plan" / "💪 Esta semana: X/Y días" / "Empieza tu racha esta semana") y vista Premium (stats Semanas seguidas/Récord/Días este mes + mensajes por estado). El resumen del mes ya no usa la palabra "racha" para días seguidos ("Encadenaste hasta N días"). workoutStreak/longestStreak (por días) SIGUEN en avi-core (usadas por el resumen del mes); no borrar. Verificado con scripts/_verify-streak.mjs.*

*Hitos sesión 2026-07-06 (parte 5 — descuento por adherencia ELIMINADO, avi-v286): decisión de Camilo ("mejor eliminar todo rastro... a nadie le importa mucho" — tuvo poca recepción con los asesorados). Fuera: gxDiscount/gxNextTier (avi-core + 7 tests → suite 267/267), tarjeta del coach en renderDetailMembership (app-6), bloque monthHTML del cliente (app-4; ya no se insertaba desde el 06-28), CSS .gx-month y familia, logro "Mes perfecto" (dependía del descuento) → reemplazado por "⭐ 50.000 kg". El NIVEL permanente y los LOGROS siguen intactos. Verificado con harness dedicado scripts/_verify-gamif.mjs (nivel+logros pintan, cero rastro en cliente y coach). Aprendizaje de producto: incentivos de dinero por adherencia no movieron la aguja con esta base de usuarios; NO reconstruir sin pedido explícito.*

*Hitos sesión 2026-07-06 (parte 4 — CI + SW + contraste + PUSH RESUCITADO, avi-v284/v285): **CI en GitHub Actions** (.github/workflows/ci.yml: suite + el MISMO script del hook en cada push/PR) + check 10 del hook (?v= de index.html debe = CACHE_NAME del sw.js — estuvo 37 versiones desalineado). **v284**: precache del SHELL con ?v=NNN (antes nunca matcheaba → instalación fresca offline sin JS), fallback ignoreSearch, cdn.jsdelivr.net cacheado (supabase-js: el login moría offline), contraste AA de --t3 (claro B0B0B0→757575, oscuro 4A7A58→669B74; jerarquía t1>t2>t3 intacta). **v285 — PUSH ESTABA MUERTO desde el cutover a auth (~2026-06-02)**: subscribePush solo corría en tryAutoLogin (legacy) → ningún dispositivo se re-registraba y los push dirigidos (chat) no encontraban suscripción. Re-conectado a _enterAuthSession (cliente, 4s post-init) y _enterCoachAuth ('_coach', 3s). RLS de push_subscriptions cerrada: anon FUERA (podía UPDATE de cualquier fila); cada quien escribe solo su fila (client_id=auth.uid(); '_coach' solo COACH_UID); subscribePush manda el token de sesión y NO marca el endpoint si el POST falla. Filas pre-auth podadas (re-registro automático al abrir la app). Verificado contra la API real: anon 401, fila ajena 403, propia 201. OJO: los asesorados recuperan push al ABRIR la app una vez (los que no la abran quedan sin notificaciones hasta entonces). Banner de inactivos + whatsappNudge YA EXISTÍAN (backlog viejo estaba desactualizado). legal/: borradores de consentimiento SIN conectar al registro (decisión de Camilo pendiente).*

*Hitos sesión 2026-07-06 (parte 3 — regresión del atrás CAZADA, avi-v283): la falla de `_test-coach-back` era real pero el culpable NO era la navegación: **`_enterAuthSession` corría DOS veces** — doLogin la ejecuta al entrar, y el BOOT (cadena async `syncFromCloud→getSession`) llegaba TARDE a su chequeo, encontraba la sesión recién creada por doLogin y RE-ENTRABA completo (segunda carga de red + segundo initClientView cuyo `navReset` tardío arrasaba `AVINAV.stack`: 2 entradas → 0 → el siguiente atrás no volvía a Inicio). Se cazó con un Proxy espía sobre AVINAV.stack (quién muta, con stack trace). En prod le pasa a un usuario con red lenta + autofill que loguea antes de que el boot termine (el harness dispara la carrera siempre). FIX: guard de idempotencia en el boot (`!AUTH_MODE` antes de re-entrar; si el login manual ya entró, solo marca authEntered). Verificado: `_test-coach-back` 20/20, `_repro-back-v243` 17/17, smoke OK, 274/274. GOTCHA re-confirmado: correr varios harnesses seguidos rate-limita el login de samuel (GoTrue) → setups de G1 fallan en cascada; esperar ~2 min entre corridas.*

*Hitos sesión 2026-07-06 (parte 2 — gates QA resucitados + harnesses E2E al repo): **el pre-commit hook llevaba SEMANAS sin correr**: `core.hooksPath` (local, .git/config) apuntaba a `Desktop\apex\...` (ruta pre-mudanza, muerta) → git no corría NINGÚN hook; y aunque corriera, el script buscaba apex-core.js/apex.test.js (pre-rename) y auditaba el inline de index.html (pre-modularización). FIX: `core.hooksPath=scripts/hooks` (la copia versionada ES el hook vivo, cero drift; al clonar en otra máquina correr ese git config una vez) + hook reescrito para los 9 módulos reales (633 funciones, 320 IDs, 161 handlers, 212 definiciones de catálogo; suite obligatoria — antes solo warn). Check 6 cuenta solo definiciones `id:'eN',name:` (las referencias sin name de entrenos rápidos/warmups son repetibles). Check 7 ahora también caza las creds E2E de prueba. El skill avi-audit ya no duplica lógica: ejecuta `python scripts/hooks/pre-commit` (una sola fuente de verdad). **Harnesses E2E versionados en scripts/e2e/** (16 + README con cobertura y gotchas; creds externalizadas a `%USERPROFILE%\.avi\e2e-creds.json` vía `_creds.mjs` — el repo es público). Al resucitarlos: `_test-coach-back.mjs` (20/20 en v236) hoy falla 2: el atrás desde p-detail consume 2 entradas del stack (2→0) → siguiente atrás no vuelve a Inicio. REGRESIÓN pendiente de cazar (introducida v237–v282, sospecha: unificación guiado).*

*Hitos sesión 2026-07-06 (telemetría de errores, avi-v282): los errores de producción fallaban EN SILENCIO (caso Luz). Tabla `app_errors` (migración `app_errors_telemetry`: RLS insert-only anon/auth con guardas de tamaño, select solo coach, poda pg_cron `avi-errors-prune` 30 días + tope 5000 filas) + `errReportGate` PURA en avi-core (dedupe firma 120 chars, 5/sesión, 20/día; 7 tests, 274/274) + `_logAppError` y listeners error/unhandledrejection try-TOTALES en app-1-infra (hallazgo Lucas QA: un reason con toString que lanza tumbaba al propio listener — corregido pre-deploy). `build` = CACHE_NAME del SW leído de caches.keys() (sin constante que mantener). Tope diario en localStorage `ax_errday` (NO va en SB_KEYS, sobrevive logout a propósito). Verificado E2E headless real: 2 POST 201 → filas en Supabase; duplicado no re-postea. Consultar errores: `select * from app_errors order by at desc` (solo coach/service). ⚠️ HALLAZGO de proceso: el pre-commit hook aún apunta a apex-core.js/apex.test.js (renombrados) → core y suite se SALTAN en silencio desde el rename; ni el hook ni avi-audit conocen los app-*.js. Pendiente repararlos (tarea de la sesión). MISMA SESIÓN: respaldo doble de datos (ver sección Respaldos): pg_cron diario en DB + export local programado en la PC.*

*Hitos sesión 2026-07-03 (cronómetro isométrico distinguible del descanso, avi-v245): reporte de Camilo 2026-07-02 — en cardio/HIIT, al tocar ▶ de la plancha "aparece como si ya hubiera terminado". Reproducido con harness CDP (`Desktop/AVI/_repro-plancha.mjs`): la MÁQUINA DE ESTADOS estaba bien (la serie NO se marcaba al tocar ▶; 267 tests + matriz isométrico/HIIT × clásica/guiado verde); el bug era de PERCEPCIÓN: el crono del isométrico reusaba el banner VERDE del descanso ("Aguanta" en letra chica + botón "Saltar ⏭") → se leía como "serie hecha, descansando"; la fila no mostraba NINGÚN estado "en curso"; al llegar a 0 el ✓ se marca solo (by design) → "la marca apareció sola"; y "Saltar" durante la plancha cancelaba EN SILENCIO sin marcar (en guiado hasta lanzaba tarjeta de "siguiente"). FIX en ambos modos: banner/overlay ÁMBAR (`.restbanner.hold` / `.gm-rest-overlay.gm-hold`, fijo en claro+oscuro) con "💪 ¡Aguanta la posición!" y sub "se marca sola al llegar a 0"; fila clásica `.holding` resaltada con el ▶ convertido en cuenta ("42s", re-consultado por id en cada tick → sobrevive re-renders del poll); 2º tap en ▶ o botón "⏹ Cancelar" detiene SIN marcar con toast honesto (guard 800ms anti doble-tap; estado `HOLD` en app-4 / `GM.holding` en app-6; restauración `_endHoldUI`/`_gmEndHoldUI` llamada desde startClientRest/resetSession/gmShowRest/closeGuidedMode y desde el ✓ manual de la misma serie); ▶ con SEG vacío ya no es botón muerto (toast instructivo). HALLAZGO de Lucas QA corregido pre-deploy: startHoldTimer hacía solo `clearInterval(restInt)` sin `_stopRest()` → el listener `_restVis` de un descanso previo quedaba vivo y, al volver de segundo plano con su endAt vencido, MATABA el hold (banner desaparecido, toast "¡A por la siguiente serie!", serie nunca marcada) — ahora `_stopRest()` desmonta el listener (regresión S1z en el harness: descanso previo → hold → visibilitychange). Datos reales auditados en Supabase: planchas isométricas de todos sanas; "Andres Martínez" (user 0a6484ed…) = el propio Camilo. GOTCHAS del harness: `cnTodayGuard` salta el re-render de Hoy (llamar `renderClientToday(c)` directo tras inyectar rutina); el poll de 15s PISA la rutina inyectada (stub `UD.loadOwn=async()=>null`); logins repetidos de samuel@apex.com se rate-limitan (perfil Chrome persistente). DECISIÓN de Camilo (misma sesión): unificar dejando el MODO GUIADO como pantalla principal de "Hoy" — plan de ejecución por fases (F0 baseline → F1 paridad → F2 embebido con flag `ax_ui_guided` → F3 atrás/TWA → F4 default → F5 retiro de la clásica) en **`docs/plan-unificacion-guiado.md`**; leerlo COMPLETO antes de empezar esa migración.*

*Hitos sesión 2026-07-02 (retorno de "Conectar mi Google" + assetlinks raíz, avi-v244): caso Luz — su vínculo Google SÍ quedó (identidad creada 12:27 en Supabase) pero la app no dijo NADA, y al volver de Google la app se veía "web con letras chicas". Dos causas raíz: **(1) assetlinks.json faltaba en la RAÍZ del dominio** — Android verifica la pertenencia del TWA SOLO en `https://kronos-apex.github.io/.well-known/assetlinks.json` (la copia bajo /apex-app/ NO cuenta) → verificación fallida → Chrome muestra barra de navegador. FIX: commit 625264b en el repo raíz `Kronos-apex/kronos-apex.github.io` (assetlinks + `.nojekyll`), verificado 200 en prod. OJO: Chrome CACHEA la verificación — el celular puede necesitar reinstalar la app o reiniciar Chrome, o tardar días en re-verificar. **(2) el resultado de `linkIdentity` vuelve en el HASH de la URL** (`#error=`/`error_code=`/tokens) y `detectSessionInUrl` de supabase-js lo consume al crear el cliente → éxito y error se PERDÍAN en silencio. FIX v244: `parseOAuthReturn(hash,search)` PURA en avi-core (4 tests, 267/267) + `const _OAUTH_RET` capturada en PARSE-TIME en app-1-infra (antes de que exista el cliente); `linkGoogle()` deja miga `localStorage ax_glink_pending` antes del redirect (redirectTo intacto — NO tocar la allowlist); `_handleGoogleLinkReturn()` (app-2-login) consume la miga, limpia el hash (`history.replaceState`, no rompe el guard del atrás), y toastea: error→mensaje español 8s (identity_already_exists ahora explica el auto-saneo de cuentas fantasma v242), éxito→"✅ ¡Google conectado!" + hint "vuelve a la app desde su ícono" si no está standalone + re-render de la tarjeta; se programa con `setTimeout` ANTES del `await _enterAuthSession` (gotcha v216: ese await puede quedar pendiente). `toast(msg,ms)` acepta duración. Harness `Desktop/AVI/_repro-glink-v244.mjs` 13/13 (boot real con #error → toast+flag+hash limpio; éxito; sin flag inerte; flag viejo silencioso; toast cabe en viewport) + regresión `_repro-back-v243.mjs` 17/17. **Hallazgos de Lucas QA corregidos pre-deploy:** `#toast` era `white-space:nowrap` → los mensajes largos se SALÍAN de la pantalla en móvil (el texto "existía en el DOM" pero no se leía — clase de bug existe≠se-ve) → ahora `normal` + `width:max-content` + `max-width:calc(100vw - 32px)`; el flag lleva TIMESTAMP y se descarta en silencio si >10 min (quien abandonó en Google no recibe toast espurio días después); `access_denied` (canceló en Google) → mensaje propio, no "revisa tu internet"; el mensaje de identity_already_exists da pasos ejecutables (cerrar sesión primero). GOTCHAS del harness: navegar de APP a APP#hash es same-document (no re-parsea `_OAUTH_RET`) → pasar por about:blank; login de GoTrue falla transitorio a veces → reintentos.*

*Hitos sesión 2026-06-30 (stepping del botón atrás para el COACH, avi-v236): el cliente ya tenía stepping completo; el coach solo tenía el fallback detalle→asesorados y, fuera de eso, el atrás armaba salida desde cualquier panel. Ahora el atrás del coach recorre los paneles visitados paso a paso hasta Inicio (igual que el cliente). Implementación = espejo del patrón `cnTab`: instrumenté `gp(id,sidebarEl,pageTitle,_silent)` (app-2-login.js) para registrar en `AVINAV.stack` un undo en CADA navegación REAL hacia adelante; `_silent` (undo del stack + navegaciones "hacia atrás") solo sincroniza `AVINAV.curTab` sin registrar. Mapa `_COACH_PANELS` (id→{sbi,title,nav-móvil}) + `_coachRestore(snap)` reconstruye el panel previo SIN registrar; **p-detail recrea el detalle del asesorado abierto** (captura `CUR.clientId` en el snapshot → `openDetail(cid,true)`; si el cliente fue borrado cae a la lista). `openDetail` recibió un 4º arg `_silent` que pasa a su `gp`. `initCoach` fija `curTab` al panel visible (Inicio en login, o el último al volver de "Mi entrenamiento") en vez de `null`. Marcadas como silenciosas las navegaciones "hacia atrás": paso-3 del handler (`_aviHandleBack`, queda como red de seguridad con stack vacío), link "← Volver a asesorados" (index.html) y el `gp` tras borrar cliente (app-3). El stack del coach y el del cliente NO se mezclan (cada transición coach↔"Mi entrenamiento" hace `navReset`). Verificado E2E con harness `Desktop/AVI/_test-coach-back.mjs` (20/20 aserciones de estado): F1 paneles Inicio→Cargas→Mensajes + atrás×2 + arma-salida-en-Inicio; F2 Asesorados→detalle→atrás→Asesorados→Inicio; F2b detalle→Mensajes→atrás REABRE el detalle del mismo cliente; F3 mismo panel 2× no duplica. **GOTCHA del harness:** se prueba sobre una sesión-cliente real (samuel) forzando la pantalla del coach → el poller del cliente (cada 15s) re-ejecuta `initClientView`→`navReset` y resetea `AVINAV` durante sleeps largos (falsos negativos); FIX = `CUR.loggedAs='coach'` en el setup (en sesión-coach real ese path no corre). 258/258 tests. 🟡 falta el ojo de Camilo en su celular (TWA real). Ver [[project_avi_back_navigation]].*

*Hitos sesión 2026-06-30 (cierre habitación de Nutrición — el 🟡 que quedó del pase premium, avi-v235): la habitación de Nutrición tenía espacio muerto abajo cuando el cliente NO tiene plan de comidas del coach (estimación automática self-serve: solo kcal+macros, sin `examples`/`plan`/`avoid`). RELLENADA con guía práctica que aparece cuando `!d.examples` (caso estimación Y planes del coach con solo números): (1) **"¿Por qué este plan?"** ahora muestra el texto educativo RICO de `GOAL_WHY` también en estimación — antes solo la nota genérica de Mifflin; mapeo `client.goal`→clave vía `GOAL_WHY_KEY` (app-5-salud.js); (2) **"Cómo repartir tu día"** — distribución por comida derivada de SUS kcal/proteína (`nutMealSplit(kcal,protG,n)` PURO en avi-core.js, +3 tests: kcal reparten 100%, proteína en partes iguales, clamp 2–6 comidas); (3) **"Ideas para armar tu plato"** — 4 grupos de alimentos contexto colombiano (`NUT_PLATE`); (4) **"Para que funcione"** — 4 tips prácticos (`NUT_TIPS`). Reusa clases `.sroom-sec`/`.nutr-meal`/`.exroom-tech`/`.exroom-note` (estilo premium, sin CSS nuevo). **+ FIX de paso (todas las 7 habitaciones):** `openNutritionRoom` hacía `body.scrollTop=0` sobre `#nutroom-body`, pero el scroller real es `.sroom` (`#nutrition-room`, `overflow-y:auto`) → reabrir una habitación larga quedaba scrolleada donde se dejó. Arreglado en el chokepoint único `_roomFront` (app-4): `room.scrollTop=0` en apertura fresca (no perturba anidadas). Verificado E2E con harness CDP (`Desktop/AVI/_shot-nutri.mjs`, samuel, bypass SW): caso estimación (Ganar músculo) y caso coach (Definición) ambos llenan la vista (scrollHeight 2071) y reabren arriba. 258/258 tests. 🟡 falta el ojo de Camilo en celular.*

*Hitos sesión 2026-06-10 (auditoría completa + mejoras, apex-v136): 🔴 **BACKUP DE DATOS REALES** — `apex_weekly_backup()` solo respaldaba el blob legacy `apex_data`; `user_data` (datos reales de los 10 usuarios) estaba SIN respaldo automático → función reescrita (snapshot = `{apex_data:{...}, user_data:[filas]}`, retención 8) + ejecutada ya (snapshot #4: 10 usuarios, 315KB). ⚡ Quitado el fetch legacy de `apex_data` en `syncFromCloud()` (RLS sin policies → siempre vacío = peso muerto con timeout de 8s en el boot; la carga local/migraciones se conservan; merge* siguen en avi-core.js, testeadas). 📱 UX móvil: `.scheck` área táctil efectiva 44px (::after, sin cambio visual), `inputmode` decimal/numérico en inputs de serie (kg/km vs reps/secs), aria-labels (scheck, timer-go) + alts en fotos de progreso. 🔍 Auditoría área×área: catálogo limpio (109), suite 123/123, auth 10/10 cuentas con datos y 0 huérfanas, payload sano (index 636KB, video máx 848KB), `fb01` Remo Australiano / `fb04` PM Rumano c/ Mancuernas = customs legítimos sin ficha (oportunidad, no bug). Pendiente Camilo: activar leaked-password protection (Dashboard→Auth→Settings) y confirmar Prensa desaparecida tras reabrir.*

*Hitos sesión 2026-06-10 (anti-duplicados de raíz, apex-v131→v135): 🛡️ `scripts/audit-catalog.mjs` (dups id/nombre, campos faltantes, registros→ids inexistentes, media sin archivo) integrado al pre-commit como check [9] — un BLOCK aborta el commit. 👻 PRENSA FANTASMA raíz hallada: vivía solo en localStorage del navegador de Camilo (el sync ya no la limpiaba) → `dedupeExercises()` en boot elimina retirados/fantasma y remapea rutinas (REMOVED_EXERCISES e38→e15). 🗑️ e38 "Curl Femoral Acostado en Máquina" ELIMINADO (dup de e15) en código + Supabase (6 rutinas + ax_c remapeadas, verificado 0). 🎬 +4 videos (e40,e47,e48,e57). 🖼️ 24 fotos stock reemplazadas (lote Gemini de Camilo) + e15/e33/e36 → quedan 22 stock. 📖 Glosario 15→29 términos.*

*Hitos sesión 2026-06-10 (cont. media): 🎬 +5 VIDEOS (e30 foto trasnuca; e85 aperturas alta, e86 baja, e3 cable, e56 predicador, e33 smith) → **~28 ejercicios con video**. 🚺🚹 GENDER-AWARE reactivado SOLO para e2 y e33 (`EX_VID_BOTH` + `exVidSrc` usa `_viewSex()`; existen `.mp4`♂+`_f.mp4`♀; Camilo "muestra ambos"). 📸 e105 foto correcta → **`EX_IMG_HIDE` vacío = 110/110 fotos OK**. 🖼️ Auditadas las 110 fotos por tamaño → **46 son STOCK** (320px, no AVI) vs 64 AVI (720²); lista exportada a `Desktop/FOTOS-STOCK-POR-REEMPLAZAR.txt`. (apex-v128→v130)*

*Hitos sesión 2026-06-10 (cont.): 🎬 22 VIDEOS de ejercicio con técnica correcta (Camilo editó, Claude solo enchufó: 720² blurred-fit + sin audio; 7 nuevos e14/e26/e28/e42/e51/e52/e80 + 15 reemplazos; e36 outlier 919→748KB; apex-v125). 🆕 e110 PRESS INCLINADO CON BARRA al catálogo (faltaba; ficha premium + coachTip; apex-v126). 📸 16 FOTOS → **catálogo 110/110 con foto** (e31/e92 des-ocultadas, e110 + 9 nuevas; `process-photos.sh`; apex-v127). Suite 123/123 verde. Regla nueva: Camilo edita videos, Claude no (ver `project_avi_video_pipeline_gender`).*

*Hitos sesión 2026-06-10 (optimización móvil): 🔊 SONIDO iOS — `playRestEndBeep`/`playRestTick` creaban un `AudioContext` nuevo por beep (nace suspended → iPhone mudo); fix = contexto singleton desbloqueado en el 1er gesto + resume() defensivo (apex-v121). Solo resuelve foreground/pantalla-encendida; bloqueada sigue necesitando push nativo. 🗑️ −571 LÍNEAS de código muerto (requestNotifPermission, showPRCelebration+banner #congrats, checkInactivity, exportRoutineAsImage ~245, exportNutImage ~264 — recuperables en git; apex-v122). ⚡ FASE 2: render diferido en login (5 secciones→1 "Hoy"+badge; Perfil/Historial/Rutinas/Mensajes lazy en cnTab; behavior-preserving; apex-v123). 📦 FASE 3: login video `preload auto→metadata` (no bajaba 1.4MB de golpe); payload ya optimizado (lazy imgs, videos on-demand, sin base64 pesado); minificar descartado (rompe single-file/no-build; apex-v124). index.html 9.737→9.178 líneas. Rutas calientes (marcar serie, timers, getLog) ya eran eficientes — no se inventaron micro-opts.*

*Hitos sesión 2026-06-10: 🧹 PRE-LANZAMIENTO — quitado el disparador temporal `#cidemo` de QA (index.html, sin código muerto; apex-v120). 🔍 Auditados empty states + mini-tour: YA EXISTÍAN completos (Hoy "preparación"/"descanso", Rutinas, Progreso, Mensajes, peso/medidas/fotos + onboarding educativo 3-slides) — la bitácora estaba más pesimista que el código. 🗑️ Borradas 6 cuentas huérfanas de `auth.users` (eran 6, no 3): bluxsam7/Sam Blux, abusamadrestocino/Tocino, aviapptraining2020/AVI, andres@apex.com, andresmartinezventas2024, pavedwings38 (Camilo confirmó las 6). Quedan 10 cuentas, 10/10 con `user_data`. 🆕 FABLE 5: recomendé NO implementarlo en AVI (AVI no tiene IA in-app; key nunca en cliente→requeriría Edge Function; $10/$50 MTok es el más caro; el coach humano real es la ventaja vs Gravl). Fase C auditada = efectivamente completa.*

*Hitos sesión 2026-06-09 (cont. 14): 🎨 REDISEÑO INTERIOR ✅ COMPLETO — Progreso consolidado (pestaña "Historial"→"Progreso": gamificación nivel+descuento movida de Perfil al tope + gráfica + sesiones `.sescard` repulidas; apex-v118) y Perfil/seguimiento premium (`.prof-collapse` + `#cn-profile .ctitle` Anton esmeralda, scoped, sin afectar al coach; apex-v119). Sin tocar lógica de logging ni cálculo de gamificación.*

*Hitos sesión 2026-06-09 (cont. 13): 📸 15 fotos AVI nuevas + e63 hollow body (Camilo renombró por ejercicio, Claude mapeó al catálogo y procesó — NUEVO flujo: Claude NO identifica visualmente). 🎬 videos e1 banca, e2 inclinado ♀, e24 pullover, e23 press militar máquina (1 rep en bucle, endpoints iguales). 🔍 AUDITORÍA de las 97 fotos → halló duplicados/contenido errado: e42=e43=e92 (misma foto barra), e31=e105 → `EX_IMG_HIDE`=[e92,e31,e105] (muestran ícono); e43 ya recuperada. e4 dominadas = dead hang (regenerar). (apex-v114→v117)*

*Hitos sesión 2026-06-09 (cont. 12): 🎬 VIDEOS sexo-agnósticos (Camilo: 220 videos ♂+♀ inviable) — `exVidSrc(e)` ignora el sexo, 1 video por ejercicio para todos (apex-v112). 🗑️ pantalla vieja "Cuéntanos de ti" (`#profile-ob`) eliminada — salía antes del wizard premium; aprovisionamiento siempre genera con defaults (apex-v113).*

*Hitos sesión 2026-06-09 (cont. 11): 🟡 rutina del día resaltada en ORO en la lista de Rutinas (`.rc-today`, apex-v111). 💬 Feedback fuerte de Camilo ("estos errores no pueden seguir pasando") → memoria `feedback_no_duplicados_auditar` (al reemplazar algo ELIMINAR lo viejo; verificar contenido↔etiqueta; auditar proactivamente).*

*Hitos sesión 2026-06-09 (cont. 10): ✨ check-in `#data-ob` rediseñado premium full-bleed (apex-v109) + disparador temporal `#cidemo` para QA sin login (apex-v110, QUITAR antes del lanzamiento) + skill `frontend-design-review` instalada.*

*Hitos sesión 2026-06-09 (cont. 9): 🎨 REDISEÑO INTERIOR arrancado (diseño final pre-lanzamiento). Skills cargadas: `frontend-design` + instalada `frontend-design-review` (Microsoft). Preview v2 `_preview-interior.html` (6 pantallas, "Noir Esmeralda") construido y APROBADO por Camilo en cel. Metodología: preview→aprobar→portar→QA. Disparador temporal `#cidemo` para previsualizar el check-in sin login (apex-v110, QUITAR antes del lanzamiento).*

*Hitos sesión 2026-06-09 (cont. 8): ✅ CHECK-IN inicial rediseñado y PORTADO a prod (`#data-ob`/`.dob-*`, apex-v109). Era el modal plano "¿Cómo estás hoy?" (peso/medidas/foto) que se veía de bajo nivel (Camilo lo fotografió). Ahora full-bleed Noir: badge gradiente+glow, eyebrow Anton, `dob-cta` gradiente, stepper de peso (±), entrada animada por paso. Estructura/handlers intactos. QA con skill (3 pilares).*

*Hitos sesión 2026-06-09 (cont. 7): 🐛 Camilo reportó: foto de e36 era hack squat (corregido cont.1) + el check-in feo. Confirmado que el wizard de registro (7 pasos) SÍ es premium; lo viejo era solo el `#data-ob`. `migrateExercises` + biblioteca: ver cont.2.*

*Hitos sesión 2026-06-09 (cont. 6): 💨 RECORDATORIOS DE RESPIRACIÓN en el modo guiado (NO en la ficha — "ahí se pierden", Camilo). `breathCue(ex)`: cue brace/Valsalva para grandes compuestos (sentadilla/peso muerto/press banca/militar/hip thrust/RDL+variantes) + default por tipo (aislamiento/cardio/isométrico…). Línea corta '💨…' en el descanso (se repite cada serie) + tarjeta `gmShowStartCard` con el cue largo (glossarizado) al abrir el guiado y al entrar a un ejercicio nuevo. (apex-v108)*

*Hitos sesión 2026-06-09 (cont. 5): 📖 GLOSARIO tap-to-explain — términos técnicos (omóplato, bisagra de cadera, protracción, retracción, isométrico, excéntrico, aducción/abducción, propiocepción, manguito rotador, serrato, romboides, sóleo, gastrocnemio) resaltados en la guía + nota de coach; al tocar → popover en lenguaje simple. `GLOSS`+`glossarize()` XSS-safe, frase-larga-gana, sin lookbehind (compat iOS). (apex-v107)*

*Hitos sesión 2026-06-09 (cont. 4): 🖼️ SPLASH más vivo — `loading-bg.jpg` grade v2 (sombras levantadas+saturación), gradiente claro al centro (se ve el modelo) y oscuro solo abajo; texto bajado a `flex-end`+`padding-bottom:9vh`. Original respaldado. (apex-v106)*

*Hitos sesión 2026-06-09 (cont. 3): 🎬 VIDEOS gender-aware — sistema nuevo `EX_VID_F`+`exVidSrc(e,sex)` (espeja `EX_IMG_F`/`exImgSrc`). Videos ♀: e33 Smith, e34 Peso Muerto, e37 extensión cuádriceps (blurred-fit, no cortar técnica), e58 hack — recorte de entrada sucia, ~5s loop, 720², grade, sin audio; pósters `_f` + a `EX_IMG_F`. Foto ♂ de e58 mejorada con el hack squat con marca AVI (antes stock). Ver `project_avi_video_pipeline_gender`. (apex-v106)*

*Hitos sesión 2026-06-09 (cont. 2): 🛠️ BIBLIOTECA DESFASADA (causa raíz) — `migrateExercises` solo agregaba, nunca refrescaba → ~70 ejercicios viejos sin descSimple/muscleLabel. Ahora REFRESCA campos de catálogo desde el código (conserva sets/reps/env). `openExDetail` cae al snapshot de la rutina si el ejercicio no está en la biblioteca del cliente (`ax_e` NO sincroniza a clientes → el ❓ ya no abre vacío) + la guía nunca queda en blanco. (apex-v106)*

*Hitos sesión 2026-06-09 (cont. 1): 🦵 PRENSA e36 — Camilo halló "Prensa" duplicada/fantasma (custom uid `mp69…`, sin guía) asignada a diana y Nataly, y la foto de e36 era en realidad un hack squat. Fix: foto real + video 720² para e36 (a `EX_VID`); foto vieja (hack con marca AVI) → e58. Datos en prod: borrada la fantasma de `ax_e` + rutinas de diana/Nataly remapeadas a e36 vía Supabase (conservando sus reps). (apex-v106)*

*Hitos sesión 2026-06-08 (cont. 20): 🔙 BUG botón atrás no cerraba el detalle del ejercicio — el handler de popstate no contemplaba `#exdetail-bg` ni `#ex-lightbox`. Añadidos al inicio del handler (lightbox→detalle→guided→modal→p-detail). 🔍 video del detalle ahora TOCABLE → abre el lightbox y se ve COMPLETO sin recorte (nuevo `openExVid` + `<video id=exlb-vid>` + `.exlb video` CSS; closeExImg resetea ambos). (apex-v105)*

*Hitos sesión 2026-06-08 (cont. 19): ⏱️ pantalla de carga +1s (Camilo) → hold final 2800/3200ms. (apex-v104)*

*Hitos sesión 2026-06-08 (cont. 18): ⏱️ pantalla de carga dura más — el hold final pasó de 100/350ms a 1800/2200ms (sesión/registro) para que se vea la marca + se lea el mensaje (Camilo: "no dura ni un segundo"). (apex-v103)*

*Hitos sesión 2026-06-08 (cont. 17): 🖼️ `media/loading-bg.jpg` (fondo de la pantalla de carga) reemplazado por foto nueva de Camilo (sentado en banca, AVI, neón "DISCIPLINA ENFOQUE RESULTADOS"). Procesada: delogo de la ✦ (caja x1315 y2510 w200 h205) + escala 900×1600 + JPG q3 (~95KB). Reemplaza la foto vieja del jalón. SW bump invalida la caché de la imagen. (apex-v102)*

*Hitos sesión 2026-06-08 (cont. 16): 🎬 hero-montage del login ajustado — Camilo no convencido con la técnica del clip press hammer (e84): reemplazado por e9 curl con barra. Montaje ahora = e13,e5,e22,e9,e6 (pierna→espalda→hombro→bíceps→espalda). El video de detalle e84 sigue intacto. (apex-v101)*

*Hitos sesión 2026-06-08 (cont. 15): 🎬 `media/hero-montage.mp4` (fondo del login cinematográfico) REGENERADO con nuestros videos AVI — montaje vertical 720×1280/30fps/11s/1.4MB de 5 clips (e13 sentadilla, e5 remo, e22 press militar, e84 press hammer, e6 jalón) escalados a cover + concat. Antes era genérico. La regla SW `.mp4` network-first ya sirve la versión nueva. (apex-v100)*

*Hitos sesión 2026-06-08 (cont. 14): 🎨 íconos de la barra inferior del asesorado (`.cntab .ctico`) de EMOJI a SVG de línea propios (Hoy=mancuerna, Rutinas=lista, Mensajes=burbuja, Historial=barras, Perfil=persona). Heredan color por `currentColor` (gris t3 → esmeralda al activar) + stroke-width sube en `.on`; badge de mensajes intacto (`#msg-badge` sigue con data-count). (apex-v99)*

*Hitos sesión 2026-06-08 (cont. 13): 🎬 +2 videos limpios (logo AVI correcto) — e22 Press militar con mancuernas + e84 Press en máquina hammer. EX_VID = e5,e6,e9,e11,e13,e22,e27,e84 (8 videos) (apex-v98).*

*Hitos sesión 2026-06-08 (cont. 12): 🎬 +1 video — e27 Jalón al pecho agarre neutro (back-view, limpio). EX_VID = e5,e6,e13,e11,e9,e27 (apex-v97). ❌ e28 (jalón agarre cerrado) NO desplegado: el video tiene el LOGO ROTO ("ANGLE/ZIBLE", no AVI) → Camilo lo regenera. e9 quedó con logo "AVi" (aceptable). Regla firme: revisar el logo de CADA video, Veo rompe el texto a veces.*

*Hitos sesión 2026-06-08 (cont. 10): 🎬 PILOTO de VIDEO de ejercicio — 4 videos generados por Gemini/Veo a partir de nuestras fotos (e5 remo, e6 jalón, e13 sentadilla, e11 tríceps). Procesados con ffmpeg: recorte del morpheo inicial (-ss 2), crop centrado a 720² (de 1280×720), sin audio, H.264 faststart (~400KB c/u) → `media/exercises/eN.mp4`. Registro `EX_VID` + `exVidSrc()`; `_showExSheet` reproduce `<video autoplay loop muted playsinline>` con la foto de `poster` en el detalle (la lista sigue con foto). SW: `.mp4` network-first (range requests iOS). Descartado el de Vidu (marca de agua). Pendiente: ver en celular y, si OK, seguir con los 109. (apex-v95)*

*Hitos sesión 2026-06-08 (cont. 9): 👋 cabecera del "Hoy" rediseñada — saludo GRANDE con el nombre (`#cn-today-head`/`renderTodayHead`) + chip de RACHA 🔥 (días de calendario consecutivos con sesión, terminando hoy/ayer). Nueva fn `workoutStreak(sessions,now)` en avi-core (testeada, +5 tests → 123). 🧹 3 cuentas de prueba borradas de `user_data` por SQL (Tocino AbusaMadres, Sam Blux, AVI); quedan 8 clientes reales. (apex-v94)*

*Hitos sesión 2026-06-08 (cont. 8): 🎨 hero del "Hoy" (`.wohero`) ahora con FOTO de marca (`media/brand/ob-1.jpg`, agarre de peso muerto) a la derecha + degradado oscuro a la izquierda (texto legible) — cierra el gap visual con onboarding/finish full-bleed. 🆓 cliente LIBRE puede editar su propia rutina: flag `canEdit=COACH_SELF||isFreeClient(client)` en `renderClientAllRoutines` → "+ Nueva rutina" + ✏️/🗑️ + "Crear rutina manual" en empty state; refresco al guardar/borrar gateado por `CUR.loggedAs==='client'`. Las gráficas siguen Premium (gating intacto); los ejercicios/edición ya están disponibles para quien solo quiere registrar. (apex-v93)*

*Hitos sesión 2026-06-08 (cont. 7): 🐛 BUG el coach no podía editar SU PROPIA rutina en "Mi entrenamiento" (sí la de asesorados). Causa: la vista de asesorado (`renderClientAllRoutines`) no expone editor a propósito, y "Mi entrenamiento" la reusa → coach sin botones. Fix UI: con `COACH_SELF` se muestran "+ Nueva rutina" + ✏️ Editar/🗑️ por tarjeta (reusan openNewRoutine/openEditRoutine/delRoutine; DB.clients=[coach] ya apunta a su fila, persistencia vía upsertOwn ya estaba OK) + refresco de su vista al guardar/borrar (apex-v92).*

*Hitos sesión 2026-06-08 (cont. 6): 🐛 BUG usuarios eliminados reaparecían — al borrar un cliente desde el panel, en modo auth la fila `user_data` quedaba en la nube y volvía al re-loguear. Fix: política RLS `user_data_delete_coach` (DELETE si `coach_id = auth.uid()`) + `UD.deleteClientRow()` + `delClient()` ahora borra la fila en la nube (apex-v91). 🎬 99 prompts de video (imagen→video) en `Desktop/PROMPTS-VIDEO-EJERCICIOS.txt`. 📋 faltan 10 fotos de ejercicio: e45,e63,e72,e74,e75,e76,e90,e91,e108,e109.*

*Hitos sesión 2026-06-08 (cont. 5): 👨 7 upgrades de foto de hombre aplicados (e6,e11,e13,e71,e77,e83,e85 → default {id}.jpg, frame más representativo por ejercicio) (apex-v90)*

*Hitos sesión 2026-06-08 (cont. 4): 👩 gender-aware Fase 2 — 21 fotos de ejercicio MUJER (`{id}_f.jpg`, EX_IMG_F=24 ids) + 4 de hombre (e36/e4/e47/e94); e89/e93/e94 añadidas a EX_IMG_NAME (faltaban, trampa tipo e97). Claude identificó cada foto a full-res contra DB.exercises. Lección: verificar catálogo real, no EX_IMG_NAME (apex-v89)*

*Hitos sesión 2026-06-08 (cont. 3): 👩 fotos de ejercicio GENDER-AWARE (la asesorada ve la demo en mujer) — `exImgSrc(e,sex)` + `EX_IMG_F` + `_viewSex()`, sin tocar call-sites; Fase 1 = 3 fotos mujer (e2_f/e26_f/e50_f). Pendiente Fase 2: mapear las 27 fotos de mujer sueltas (necesitan ID) + 7 upgrades hombre. 43 fotos en `fotos-seleccionadas/` (apex-v88)*

*Hitos sesión 2026-06-08 (cont. 2): cierre de entreno con ⏱️ Duración (startedAt) + 🔥 Calorías (MET·peso·h) + 📅 Fecha (referencia: captura real Gravl "Detalles del entrenamiento") · ⭐ "¿Cómo te sentiste?" 5 caritas (feeling 1-5) que el COACH ve en el historial (foso vs Gravl IA) · identidad propia (foto full-bleed + personal + esmeralda) (apex-v87)*

*Hitos sesión 2026-06-08 (cont.): 🏆 pantalla "Fin de entrenamiento" full-bleed (overlay #workout-finish al 100%: foto + 🏆 + series/volumen/récords + PRs + confetti); primer momento full-bleed fuera del onboarding; default foto = ob-2 (pendiente foto de victoria dedicada vía Gemini); reemplaza el banner inline #congrats (apex-v86)*

*Hitos sesión 2026-06-08: 🧹 eliminada la "Nota del coach" duplicada (quitada la vieja gris "Notas técnicas (coach)" `#exd-tech-wrap`, queda solo el cue esmeralda `EX_COACHTIP`) · 🎨 onboarding intro rediseñado a FULL-BLEED inmersivo (foto a pantalla completa + texto abajo; antes cajita de 188px) · 📸 3 fotos de marca AVI con logo horneado en la camiseta por Gemini, 1536×2752 nativo + ✦ eliminado (ob-1 peso muerto, ob-2 de pie, ob-3 sentado); Claude solo delogo+grade (regla: Gemini hace el logo, no Claude) (apex-v85)*

*Hitos sesión 2026-06-07 (cont.): 📸 fotos de ejercicio lote 2 (23: 22 reemplazos + e97 Pike Push-up; e97 faltaba en EX_IMG_NAME) (v80) · 🔧 fix e9 (mostraba mancuernas, archivo Gemini mal nombrado→barra real) + 4 fuentes verticales reprocesadas con fit+relleno (no recorte; el calf raise perdía los pies); process-photos.sh detecta orientación (v81) · ⭐ NUEVA FEATURE "Nota del coach" por ejercicio (callout esmeralda visible al asesorado; mapa `EX_COACHTIP` resuelto por id sin migración; 109/109) (v82 lote 1→v83 completo) · 📝 22 descripciones cortas enriquecidas, 87 ya sólidas se dejaron (v84) · lección: verificar CONTENIDO de cada imagen, no el nombre del archivo*

*Hitos sesión 2026-06-07: 🚀 wizard+fotos de marca a prod (apex-v65) · 🐛 fix registro Google (pierde-datos→reveal, v66) · 📸 primeras 10 fotos de ejercicio AVI (v67) · 🎨 rediseño interior DARK Noir por defecto + hero/tarjetas/detalle/callouts (v68-v71,v79; causa raíz = interior en tema claro) · 🔴→✅ BUG CRÍTICO solicitudes de coach invisibles por `coach_id=NULL` + RLS → todos los self-reg al pipeline + backfill (lead real Cristhian recuperado, v72/v73) · 📝 LAS 109 fichas de ejercicio a estándar premium (músculo nombrado + criterio, v74-v78) · flujo fotos definitivo: GEMINI hace look+logo, Claude solo ✦+720²*

*Hitos sesión 2026-06-05: 📸 fotos de marca AVI (Camilo) integradas en hero/reveal/onboarding del wizard vía grade Noir Esmeralda + ffmpeg → `media/brand/` (316 KB las 4) · 4 ediciones quirúrgicas en index.html sobre hooks ya existentes · ⚠️ TODO SIN COMMITEAR Y SIN DEPLOY (arrastra el wizard de 7 pasos previo) · captura en navegador bloqueada por RAM (no se tocó el Chrome del usuario) · ver sección "Sesión 2026-06-05 — RETOMAR AQUÍ"*

*Hitos sesión 2026-06-04: 📧 SMTP propio (Brevo, remitente verificado aviapptraining2020@gmail.com) + "Confirm email" ACTIVADO + plantillas de correo en español con branding AVI (4) · todo config server-side vía Management API (sin cambios de código; la app ya soportaba confirm-email) · registro público DESBLOQUEADO · ⚠️ `smtp_port` debe ser STRING y un PATCH parcial de `smtp_*` borra el bloque entero*

*Hitos sesión 2026-06-03 #2: 🔒 FASE 4 — candados RLS cerrados (eliminadas las 4 políticas `USING(true)` de apex_data + push_subscriptions; fuga crítica anon resuelta) · respaldo legacy de `doLogin` eliminado (login = SOLO Supabase Auth) · migración blindada verificada cliente por cliente + Nataly reconciliada (cero pérdida) · Astrid/Nataly con claves Auth nuevas (`Astrid2026`/`Nataly2026`, Camilo las reparte) · 5 cuentas de prueba borradas · push_subscriptions solo-escritura (tradeoff para no romper registro) · COACH_UID = 0a6484ed-… · caché apex-v63→v64*

*Hitos sesión 2026-07-03-b (unificación guiado — F0 + F1·P10/P2/P1, avi-v246/v247): arrancó la ejecución del plan `docs/plan-unificacion-guiado.md` (progreso vivo en su sección 3b — actualizarla al cerrar cada tajada). avi-v246 (commit c61caa0): `prepareTodaySession(routine)` = reset diario + `_rehomeOrphanDropsets` al entrar a "Hoy", común a clásica y guiado (se llama desde `renderClientToday` y `openGuidedMode`; `checkAndResetSession` SALIÓ de `renderClientExList`); `finishSessionEarly`/`resetSession` devuelven boolean y el guiado los reusa vía `gmFinishEarly`/`gmResetSession` + fila `#gm-session-actions` al final de `#gm-body`. avi-v247: check-in de ánimo DENTRO del guiado — `moodChooserHtml(client,fnName)` con allowlist ('gmPickMood'|'pickMood'), `gmRender` antepone el chooser si hoy no eligió, `gmPickMood` = `pickMood` + `openGuidedMode()` (reconstruye GM.steps sobre la rutina ADAPTADA y recalcula el paso). Harness `_repro-plancha.mjs` ampliado a **31 checks** (S5: P2 reiniciar/finalizar + P10 día nuevo sin render clásico; S6: chooser en guiado). QA avi-v247: Julián 🟢 6/6 y Lucas 🟢 5/5 (anotaciones leves: tarjeta de respiración re-aparece tras elegir ánimo — defendible; emoji 50px con texto XL a 360px, pre-existente; precache SHELL sin query vs `?v=244`, pre-existente). El puerto 9266 quedó ZOMBI 3 veces en la sesión — matar el PID antes de CADA corrida del harness.*

*Hitos sesión 2026-07-03-c (unificación guiado F1·P3, avi-v248): reordenar ↑↓ y sustituir 🔄 ejercicios DESDE el modo guiado (antes solo en la clásica). Refactor: el cuerpo de reconstrucción de `openGuidedMode` se extrajo a `gmRebuild()` (regenera GM.steps/currentStep desde CUR.activeRoutine sin re-lanzar la tarjeta de inicio); `gmMoveEx`=`todayMoveEx`+`gmRebuild`; la botonera `.cex-reorder` se añadió al `.gm-ex-header`; 🔄 reusa `todaySubstitute`→`#m-picker` y `_applySubstitute` reconstruye el guiado si sigue abierto; `gmPickMood` migró a `gmRebuild` (elimina la tarjeta de inicio duplicada que anotó Lucas en v247). BLINDAJE: `gmRebuild` cancela timers vivos (GM.restTimer/holding/hiit) antes de repintar — el HIIT corre dentro de la tarjeta con los ↑↓ a la vista, así que moverla dejaría el intervalo huérfano; ahora se cancela (verificado con el escenario S8 del harness). Las claves de sesión (log_/done_/drop_/lastre_/wshow_) ya viajan con el ejercicio vía `_swapSessionKeys` (v176/v237), reusado tal cual. Harness `_repro-plancha.mjs` a 36 checks (S7: la plancha baja con su serie hecha + dropset + segundos; S8: mover el HIIT en curso sin timer huérfano ni rondas fantasma), 267/267 tests, jsErrors []. NOTA de proceso: a mitad de esta tajada se agotó el plan Fable 5; el QA de P3 (Julián + Lucas subagentes) se cortó por créditos y se completó la auditoría estática inline ya en Opus 4.8 (0 duplicados, handlers resueltos, .cex-reorder en CSS). Progreso vivo de la unificación en `docs/plan-unificacion-guiado.md` §3b — SIGUIENTE es F1·P4 (lastre en el guiado).*

*Hitos sesión 2026-07-03-d (unificación guiado F1·P4, avi-v249): lastre (peso añadido) para peso corporal DENTRO del modo guiado (antes solo en la clásica). Toggle "+ Lastre (peso añadido)" una vez por ejercicio de track 'reps' (reusa `lastreOn`/`toggleLastre` de app-4 → clave `lastre_<rid>_<ei>` compartida con la clásica, ya viaja en el reorden vía `_SK_EX`); `gmSetCellsHTML(track,ex,ei,si,done,gmSug,lastre)` con el flag pinta celda KG (placeholder "lastre") + REPS, usando el MISMO campo `kg` → entra al volumen exactamente igual que la clásica (paridad de datos, sin divergencia). Harness `_repro-plancha.mjs` a 39 checks (S9: toggle presente y fila solo-REPS sin activar; al activar aparece KG y el estado persiste en localStorage; serie con lastre 10kg×12 suma 120 al volumen). 267/267 tests, jsErrors []. Progreso vivo en `docs/plan-unificacion-guiado.md` §3b — SIGUIENTE F1·P12 (nota/why + banner override en el header del guiado).*

*Hitos sesión 2026-07-03-e (unificación guiado F1·P12, avi-v250): cabecera de rutina DENTRO del modo guiado. `gmRoutineHeaderHTML()` antepone en gm-body (tras el chooser de ánimo, antes de las tarjetas de ejercicio) el nombre de la rutina + pills (nº ejercicios/series/descanso) + nota del coach (💡) + bloque "POR QUÉ ESTA RUTINA" (why) + banner "elegiste esta rutina manualmente" si CUR.todayOverride. Estilos con tokens neutros porque el guiado va sobre var(--bg) (no el gradiente verde del hero de la clásica); todo el contenido de usuario pasa por esc(). En F2 (guiado embebido como pantalla principal de Hoy) esta cabecera reemplaza al hero de la clásica. Harness `_repro-plancha.mjs` a 40 checks (S10: nombre/nota/why/pills presentes). 267/267 tests, jsErrors []. Progreso vivo en `docs/plan-unificacion-guiado.md` §3b — SIGUIENTE F1·P11 (thumb de foto, baja prioridad) y luego F2 (fase grande: guiado embebido tras flag ax_ui_guided con kill-switch).*

*Hitos sesión 2026-07-03-f (unificación guiado F1 COMPLETA): P11 (foto del ejercicio en la tarjeta del guiado) NO requirió código — el guiado ya usa `exIcon`→`exImgTag`→`.exicon-img` dentro de `.gm-ex-icon` (overflow:hidden, object-fit:cover), la misma cadena que la clásica; solo se dejó el check de regresión S11 (verifica `img.exicon-img` con src `.jpg`). Con esto la FASE F1 del plan de unificación queda COMPLETA: toda la paridad del guiado con la clásica (P1 ánimo, P2 finalizar/reiniciar, P3 reordenar/sustituir, P4 lastre, P10 reset diario común, P11 foto, P12 cabecera; P5-P9/P13/P14 ya estaban). Desplegado en avi-v246→v250 (6 deploys esta jornada). Harness `_repro-plancha.mjs` = 41 checks (S1-S11), jsErrors []. SIGUIENTE = F2 (fase grande, en sesión fresca): el guiado embebido como pantalla principal de "Hoy" detrás del flag `ax_ui_guided` (kill-switch), partida en sub-deploys (flag+toggle Perfil → render embebido → poll). Plan y progreso vivo en `docs/plan-unificacion-guiado.md` §3b. 🟡 Falta el ojo de Camilo en el celular recorriendo el guiado.*

*Hitos sesión 2026-07-03-g (unificación guiado F2 sub-deploy 1, avi-v251): arranca la FASE F2 (el guiado como pantalla principal de "Hoy"), partida en sub-deploys detrás de un kill-switch para no arriesgar producción. Sub-deploy 1 = flag `ax_ui_guided` por dispositivo: `uiGuided()`/`setUiGuided(on)` (app-1-infra.js; NO en SB_KEYS → localStorage puro, no sincroniza a la nube); `_initUiGuidedFromUrl()` parsea `?uig=1`/`?uig=0` en el boot (app-2-login.js tras initTextSize); `renderGuidedViewToggle` (app-4) llena `#cn-guided-card` en el Perfil del cliente SOLO cuando el flag ya está ON, con botón "↩ Volver a la vista clásica" (switchToClassicView) — oculto por defecto para no confundir a los demás clientes mientras la prueba es de Camilo. **INERTE con OFF**: todavía nada lee el flag para cambiar el render, así que "Hoy" sigue siendo la tarjeta clásica exactamente igual. Cómo lo prueba Camilo: abrir la app con `...apex-app/?uig=1` en SU celular. Harness `_repro-plancha.mjs` a 46 checks (S12: OFF por defecto, setUiGuided persiste, toggle aparece con ON y desaparece al volver a clásica, con OFF Hoy sigue clásica), 267/267 tests, jsErrors []. GOTCHA harness reconfirmado: antes de correr, matar TODOS los Chrome del harness (no solo el listener del 9266) — si queda un Chrome zombi el setup falla en falso ("Cannot set properties of undefined (setting 'routines')" = login no completó). SIGUIENTE = F2 sub-deploy 2 (el render embebido, lo grande). Progreso vivo en `docs/plan-unificacion-guiado.md` §3b.*

*Hitos sesión 2026-07-03-h (unificación guiado F2 sub-deploy 2, avi-v252): el RENDER EMBEBIDO — con el flag ax_ui_guided ON, "Hoy" ES el modo guiado embebido en #cn-today-body (no el hero+cex-list clásicos). `renderClientToday`, tras adaptar la rutina, si uiGuided() hace `con.innerHTML=''` + `openGuidedEmbedded(todayR)` + return. `openGuidedEmbedded` reubica el MISMO nodo #guided-mode DENTRO de #cn-today-body (reusa topbar/body/footer/rest-overlay ya probados) con la clase `.gm-embedded` que en CSS quita position:fixed, oculta el ✕, quita el scroll interno propio y pone zoom:1 (para no doblar el zoom de texto que ya aplica .cnp). Helpers `_gmCaptureHome`/`gmRestoreOverlayHome` mueven el nodo entre overlay↔tab; `gmRestoreOverlayHome` corre al TOPE de renderClientToday ANTES del innerHTML='' (si no, borraría el nodo compartido del DOM) pero SOLO actúa si está embebido — un overlay abierto NO se toca (bug corregido: ocultaba el guiado a media sesión cuando pickMood/todayMoveEx disparan renderClientToday con el overlay abierto, fallas S6/S7/S8). `closeGuidedMode` es embedded-aware: al completar TODO no oculta el tab (la celebración la muestra checkAndShowCongrats); `gmUpdateActionBtn` oculta el botón "Cerrar" en embebido. Seguridad de diseño: openGuidedMode (overlay) tiene UN solo llamador (el botón "▶ Empezar", que NO se pinta con flag ON) → nunca hay dos renders gm-* a la vez. KILL-SWITCH intacto: con flag OFF (default) nada de esto corre, "Hoy" sigue clásica idéntica. Cómo lo prueba Camilo: `...apex-app/?uig=1` en su celular; para volver, Perfil → "Volver a la vista clásica". Harness `_repro-plancha.mjs` a 50 checks (S13: embebido dentro del tab sin ✕/cex-list/Empezar, marcar serie, completar todo sin ocultar + botón Cerrar oculto, flag OFF restaura la clásica y devuelve el nodo a su sitio), 267/267 tests, jsErrors []. SIGUIENTE = F2 sub-deploy 3 (el poll de 15s re-renderizando el embebido sin matar timers vivos). Progreso vivo en `docs/plan-unificacion-guiado.md` §3b.*

*Hitos sesión 2026-07-03-i (unificación guiado F2 sub-deploy 3 — FASE F2 COMPLETA, avi-v253): el poll en vivo ya no corta la serie del embebido. Guarda al TOPE de renderClientToday: si `uiGuided() && _gmIsEmbedded() && _gmLiveTimer()` (descanso/HIIT/isométrico corriendo) hace `return` inmediato sin tocar el DOM → el _pollAuthClient de 15s (que refresca "Hoy" cuando el coach cambia el plan) no interrumpe la serie en curso; el refresco entra en el próximo render sin timer vivo. Reorden/ánimo NO pasan por esa guarda para re-render (llaman gmRebuild aparte, que cancela el timer a propósito), así que siguen ágiles. `_gmLiveTimer()` (app-6) expone GM.restTimer||GM.hiit||GM.holding. Harness `_repro-plancha.mjs` a 54 checks (S14: el HIIT del embebido sobrevive a un poll con cambio de plan, termina normal, y un render posterior sin timer aplica el plan nuevo — refresco diferido). CON ESTO LA FASE F2 QUEDA COMPLETA (avi-v251 flag+toggle, v252 render embebido, v253 poll): el guiado embebido ES la pantalla de "Hoy" detrás del flag ax_ui_guided (kill-switch, default OFF → producción intacta). AHORA TOCA CAMILO en su celular: abrir `...apex-app/?uig=1`, entrenar varios días, y confirmar; volver a la clásica desde Perfil → "Volver a la vista clásica". NO avanzar a F4 (default ON) sin su visto bueno + idealmente 1-2 clientes beta. SIGUIENTE = F3 (atrás/TWA con el embebido; correr _repro-back-v243.mjs, aún no corrido en toda la unificación). 267/267 tests, jsErrors []. Progreso vivo en `docs/plan-unificacion-guiado.md` §3b.*

*Hitos sesión 2026-07-03-j (unificación guiado F3 en código, avi-v254): botón atrás con el guiado embebido. Dos fixes en `_aviCloseTopOverlay` (app-2-login.js): (1) BUG CRÍTICO — el check del guiado era `!gm.classList.contains('hidden')`, pero el embebido (F2) tampoco está hidden → el atrás en el tab "Hoy" embebido entraba a closeGuidedMode() (que es embedded-aware y no hace nada) y quedaba SIN EFECTO (el usuario no podía volver a la pestaña previa ni armar salida); ahora el check excluye `.gm-embedded` → el embebido es un TAB y el atrás sigue el flujo normal de pestañas. (2) el descanso del guiado (#gm-rest-overlay) ahora se cierra con atrás vía gmSkipRest — antes en overlay cerraba TODO el guiado y en el embebido no hacía nada; el fix aplica a ambos modos (más intuitivo: atrás salta el descanso, no nuke la sesión). La ficha ❓ (openExDetail) y el lightbox ya empujaban su propia capa (navOpenLayer, v243) → el atrás los cierra igual en embebido. Harness `_repro-plancha.mjs` a 58 checks (S15: embebido no se cierra con atrás; descanso sí; ficha empuja capa; overlay clásico SÍ se cierra sin regresión); `_repro-back-v243.mjs` regresión LIMPIA (habitaciones/ficha/lightbox intactos). 267/267 tests, jsErrors []. 🟡 FALTA: probar el atrás en el TWA REAL de Camilo con `?uig=1` (el WebView del TWA es donde el atrás se rompe). SIGUIENTE = F4 (default ON) SOLO tras el visto bueno de Camilo entrenando en su celular con F2+F3. Progreso vivo en `docs/plan-unificacion-guiado.md` §3b.*

*Hitos sesión 2026-07-03-k (unificación guiado — interruptor en Perfil, avi-v255): Camilo pidió poder encender la vista guiada DESDE la app (el truco `?uig=1` no sirve en el TWA instalado porque no se puede editar la URL). `renderGuidedViewToggle` (app-4) ahora es un interruptor ON/OFF real en el Perfil del cliente: con la vista guiada ON muestra "↩ Volver a la vista clásica" (visible para cualquiera que la tenga encendida = kill-switch a mano); con OFF muestra "🧭 Activar vista guiada" SOLO cuando `COACH_SELF` es true (el coach en su propio entrenamiento = Camilo), para que los clientes NO lo vean todavía mientras la vista está en prueba (plan F2.5). `switchToGuidedView()` enciende el flag + re-renderiza; `switchToClassicView()` apaga. Camilo lo activa en: Mi entrenamiento → Perfil → "🧭 Activar vista guiada", luego va a "Hoy". COACH_SELF setea CUR.clientId=me.id (openMyTraining) así que el embebido y el chooser de ánimo funcionan en su entreno. Harness `_repro-plancha.mjs` S12 ampliado a 60 checks (cliente+OFF oculto; COACH_SELF+OFF muestra Activar; el botón enciende; ON muestra Volver para cualquiera). 267/267 tests, jsErrors []. Progreso vivo en `docs/plan-unificacion-guiado.md` §3b.*

*Hitos sesión 2026-07-03-l (unificación guiado — interruptor SIEMPRE visible, avi-v256): Camilo reportó que NO le aparecía ningún botón tras entrar/salir. Causa probable doble: (1) el gate COACH_SELF que le puse en v255 era demasiado estricto para su uso real, y (2) su app servía una versión vieja en caché. Fix: `renderGuidedViewToggle` ya NO depende de COACH_SELF — el interruptor se muestra SIEMPRE en el Perfil del cliente (con OFF → "🧭 Activar vista guiada"; con ON → "↩ Volver a la vista clásica"). Sigue siendo opt-in por dispositivo (default OFF = vista clásica para todos). Trade-off aceptado: ahora los clientes también verían el botón, pero la vista guiada está completa+probada y es opt-in; se puede re-esconder en 1 línea si molesta. Harness S12 (59 checks) actualizado: OFF muestra Activar, activar enciende, ON muestra Volver. 267/267 tests, jsErrors []. NOTA caché: el usuario debe forzar la versión nueva (abrir en Chrome con recarga, o Ajustes→Apps→AVI→Borrar caché, o reinstalar el TWA) — entrar/salir del ícono NO garantiza refrescar el SW. Progreso en `docs/plan-unificacion-guiado.md`.*

*Hitos sesión 2026-07-03-m (unificación guiado — el interruptor ahora SÍ cambia "Hoy", avi-v257): Camilo reportó que activar/desactivar la vista guiada NO cambiaba nada en "Hoy". Causa: `cnTodayGuard` (avi-core.js) solo re-renderiza "Hoy" cuando cambia el DÍA (`CUR.todayRenderedDay !== todayLabel`); al togglear el flag desde Perfil y volver a "Hoy", el día no cambió → la guarda saltaba renderClientToday → seguía la vista vieja. Fix: `switchToGuidedView`/`switchToClassicView` ahora llaman `_switchTodayView(on)` que setea `CUR.todayRenderedDay=null` (fuerza el re-render en la próxima entrada a "Hoy") y, si "Hoy" está visible en ese momento, re-renderiza al instante. Harness `_repro-plancha.mjs` a 61 checks (S16: activar+ir a Hoy → embebido; desactivar+ir a Hoy → clásica, recorriendo el flujo real con cnTab). 267/267 tests, jsErrors []. GOTCHA reconfirmado: la cuenta samuel@apex.com del harness se rate-limita tras ~15 corridas seguidas (login "entra" pero DB.clients vacío → setup falla "Cannot set properties of undefined (setting routines)"); esperar ~40s y reintentar. Progreso en `docs/plan-unificacion-guiado.md`.*

*Hitos sesión 2026-07-03-n (unificación guiado — el interruptor lleva a "Hoy" al instante, avi-v258): Camilo reportó que el cambio SÍ ocurría pero para VERLO tenía que salir/entrar de la app o ir al panel de coach y volver. Causa: el interruptor vive en el Perfil, así que al tocarlo seguía en Perfil y el cambio quedaba listo pero no visible hasta navegar a "Hoy". Fix: `_switchTodayView(on)` ahora NAVEGA a "Hoy" con `cnTab('cn-today',...)` (tras resetear CUR.todayRenderedDay para saltar la guarda) → el usuario aterriza en "Hoy" viendo el resultado de inmediato (activar → guiado embebido; desactivar → clásica). Harness S16 (61 checks) actualizado: click en el botón → onToday=true + embebido/clásico según corresponda. 267/267 tests, jsErrors []. Progreso en `docs/plan-unificacion-guiado.md`.*

*Hitos sesión 2026-07-03-q (avi-v261 — botones reorder ↑↓🔄 fuera de pantalla con letra grande): Camilo (molesto, con razón, por lo que se escapa de las auditorías) reportó que al agrandar la letra los botones de subir/bajar/cambiar ejercicio en el guiado quedaban casi fuera de pantalla. Causa: `.gm-ex-header` era una fila flex SIN flex-wrap, y el nombre del ejercicio (`<div style="flex:1">` SIN min-width:0) empujaba el grupo de herramientas fuera del viewport cuando el zoom de texto lo agrandaba (embebido: el zoom lo aplica `.cnp` a 1.40 en data-fs=xl; `.gm-body` está a zoom:1 para no doblar). Fix: reestructuré el header — el nombre va en `.gm-ex-nm` (flex:1 1 140px, min-width:120px, ancho mínimo legible) y las herramientas (❓ + `.cex-reorder` ↑↓🔄) en `.gm-ex-tools` (margin-left:auto, flex-shrink:0); `.gm-ex-header` ahora tiene `flex-wrap:wrap` → con letra grande el grupo de herramientas ENVUELVE a una 2ª línea (visible, alineado a la derecha) en vez de salirse. NUEVO TIPO DE CHECK en el harness (S19, 68 checks): en vez de solo verificar presencia en el DOM, MIDE el getBoundingClientRect().right/left de cada botón contra el viewport (390px) con data-fs=xl → assert 0 fuera de pantalla (worstRight 338 ≤ 390). LECCIÓN (para no repetir la queja de Camilo): las auditorías visuales deben incluir GEOMETRÍA REAL con texto grande (lg/xl) en móvil, no solo existencia de nodos — un botón puede "estar en el DOM" y estar fuera de la pantalla (misma clase de bug que el toast nowrap v244 y el CSS cache-first v230). 267/267 tests, jsErrors []. 🟡 falta el ojo de Camilo. Progreso en docs/plan-unificacion-guiado.md.*

*Hitos sesión 2026-07-03-p (avi-v260 — salto de pantalla al cambiar de ánimo + PÉRDIDA DE DATOS al reentrenar el mismo día): Camilo probó el ánimo en el celular y encontró DOS cosas. (1) SALTO DE PANTALLA: al cambiar de ánimo la pantalla saltaba hacia abajo. Causa: openGuidedEmbedded difería un gmScrollToCurrent() a 120ms (para "volver a donde ibas") que se ejecutaba DESPUÉS del scroll-al-tope de la acción de ánimo y lo pisaba (aterrizaba en el ejercicio, sc=155). Fix: el scroll-a-current diferido ahora se rastrea en un solo handle (_gmDeferScrollToCurrent, usado por openGuidedMode/openGuidedEmbedded/gmAfterHiit) y gmScrollTop() lo CANCELA antes de subir; además el scroller real en embebido es .cnbody (no #cn-today, que es un .cnp sin overflow) → gmScrollTop sube .cnbody. gmPickMood/gmChangeMood usan gmScrollTop (antes usaban gmScrollToCurrent = el bug). (2) PÉRDIDA DE DATOS (crítico): entrenó pierna en la mañana (completo, guardado) y al REINICIAR por la tarde mientras probaba el guiado, Progreso mostró SOLO el nuevo y BORRÓ el de la mañana. Causa raíz: saveSessionToHistory de-duplicaba por (routineId+día) → como las banderas 'done' del mismo día solo se limpian al reiniciar, el guardado parcial de la tarde encontró la entrada de la mañana (misma rutina, mismo día) y la SOBREESCRIBIÓ con los datos a medias. Fix: IDENTIDAD DE SESIÓN — session_id_<rid> en localStorage, acuñado (startNewSession) al arrancar una sesión fresca: día nuevo (checkAndResetSession) o reiniciar (resetSession/gmResetSession). saveSessionToHistory ahora matchea la entrada por sessionId, NO por (rutina+día) → cada serie marcada dentro de UNA sesión actualiza la misma entrada, pero un reinicio = sesión nueva = entrada APARTE. La mañana completa queda intacta; la tarde es un 2º entreno separado (regla: nunca perder un entreno hecho, [[feedback_no_duplicados_auditar]] al revés — aquí NO fusionar). Transición sin id: se adopta solo una entrada del día/rutina SIN sessionId y NO completada (misma sesión en curso), una completada NO se toca. showWorkoutFinish y el borrado de rutina del coach también contemplan el sessionId. Harness _repro-plancha.mjs a 67 checks (S18: mañana 2/2 → reiniciar cambia sid → tarde 1/2 es entrada aparte, mañana intacta; S17 P2/P3 ahora verifican sc===0 = sin salto). 267/267 tests, jsErrors []. 🟡 falta el ojo de Camilo (probar de nuevo cambiar ánimo sin salto; y entrenar 2 veces la misma rutina un día → deben quedar 2 entradas en Progreso). Progreso en docs/plan-unificacion-guiado.md.*

*Hitos sesión 2026-07-03-o (unificación guiado — el ánimo NO se queda fuera del embebido, avi-v259): Camilo: "la vista me gusta pero no podemos dejar fuera la feature de cómo te sientes". El guiado (`gmRender`, app-6-extra.js) mostraba el chooser de ánimo, pero al ELEGIR desaparecía TODO → parecía que la feature no existía (la clásica en cambio deja un banner con la adaptación + "Cambiar cómo me siento"). Fix = paridad COMPLETA (plan P1): si aún no eligió → `moodChooserHtml(_cli,'gmPickMood')`; si YA eligió → `moodBannerHtml(GM.routine.adapt,'gmChangeMood')` (título+por qué+chips de cambios+enlace "Cambiar cómo me siento"). `GM.routine.adapt` lo trae applyMood (la rutina activa ya viene adaptada desde renderClientToday). `moodBannerHtml` ahora acepta `fnName` con allowlist (igual que moodChooserHtml) — clásica sigue usando `changeMood()`, el embebido `gmChangeMood()`. Nuevo `gmChangeMood()`: limpia el ánimo del día y, si está embebido, llama `renderClientToday` (re-adapta la rutina SIN ánimo + re-embebe); si es overlay, `gmRebuild` (muestra el chooser; re-elegir re-adapta vía pickMood) — NO renderClientToday en overlay porque renderizaría la clásica DETRÁS y dejaría el guiado pegado. Harness `_repro-plancha.mjs` a **64 checks** (S17: chooser antes de elegir → banner con "Cambiar" al elegir → chooser de nuevo tras "Cambiar", SIEMPRE embebido). 267/267 tests, jsErrors []. 🟡 falta el ojo de Camilo (recordarle forzar la versión nueva por el caché del WebView). Progreso en `docs/plan-unificacion-guiado.md`.*

*Hitos sesión 2026-07-04-h (accesibilidad — quick wins tanda 2: aria-label en botones-icono del entreno, avi-v269): WCAG 1.1.1 (lectores de pantalla). Los botones SOLO-icono del flujo de entreno no tenían aria-label → un lector los anuncia como "botón" sin significado. Añadidos: gm-check del guiado (línea 396, "Marcar serie N como hecha") y gm-check aux (línea 502, "Marcar calentamiento/dropset como hecho"); exinfo-btn ❓ en app-6:336 y app-4:1392 ("Ver cómo se hace: guía y video"); wu-guide-btn 🎥 app-6:1586. YA tenían label (sin tocar): los .scheck de la clásica (role=button aria-label "Marcar serie N"/"calentamiento"/"dropset"), los ↑↓🔄 de reorder ("Subir/Bajar/Cambiar ejercicio"), el .gm-timer-go ("Iniciar cronómetro"). Cambios 100% aditivos e invisibles (no cambian el look). 267/267 tests, node --check OK, harness TODO OK, jsErrors []. ALCANCE: se cubrió el FLUJO DE ENTRENO (donde más interactúa el atleta), NO literalmente cada botón-emoji de toda la app (coach/modales quedan para una barrida futura si se quiere). QA inline. Reemplazar emojis por iconos SVG (lo que pedía la auditoría de fondo) es DISEÑO → lo define Camilo con Claude Design; esto solo pone la etiqueta accesible mientras tanto.*

*Hitos sesión 2026-07-04-g (accesibilidad — quick wins tanda 1: foco visible + inputmode, avi-v268): 2 quick wins de la auditoría UX (bajo riesgo, aditivos, sin diseño). (1) FOCO VISIBLE por teclado (WCAG 2.4.7): en styles.css, `:focus-visible{outline:2px solid var(--g2);outline-offset:2px}` + `:focus:not(:focus-visible){outline:none}` → anillo de foco SOLO con teclado (no con mouse/touch), no pisa los border-color de :focus de los inputs (outline es capa aparte). (2) inputmode en TODOS los inputs numéricos de formulario que faltaban (index.html): su-age/height, cf-age/height, ex-s/r/hiit-work/hiit-rest/hold-secs, nut-water, pay-amount → inputmode="numeric"; su-weight, cf-weight → inputmode="decimal" (tienen step 0.1). De paso se quitó un type="number" duplicado en ex-s/ex-r. Ya tenían inputmode: bw-kg, med-cintura, y las celdas del guiado. Esc-para-cerrar-modales YA existía (app-4:2934 cierra .mdbg.on; app-1:1096 lightbox/bienvenida). 267/267 tests, harness TODO OK, jsErrors []. QA inline. PENDIENTES de la auditoría (próximas tandas): aria-label en botones-emoji (superficie grande), targets táctiles ≥44px (CSS con cuidado de no romper layout), etiqueta de texto en estados solo-color, timer de descanso pausar/+15s (feature). Diseño (iconos SVG, modales→pantallas, unificar botones, estados vacíos) lo define Camilo con Claude Design.*

*Hitos sesión 2026-07-04-f (accesibilidad — reactivar pinch-zoom, avi-v267): primera acción de la auditoría de UX que Camilo generó con Claude Design (hallazgo P0 WCAG 1.4.4). El meta viewport tenía `maximum-scale=1.0, user-scalable=no` → bloqueaba el pinch-zoom en Android (iOS lo ignora desde iOS 10). Se quitaron ambos → `width=device-width, initial-scale=1.0, viewport-fit=cover`. El control propio "Tamaño de texto" (ax_textsize, Normal/Grande/Muy grande) se MANTIENE — hacen trabajos distintos (el control refluye texto; el pinch magnifica cualquier punto). detectZoomWidthScaling (sonda del CSS `zoom` para el control de tamaño) NO se ve afectado (no toca el viewport). Cambio de 1 línea, reversible. 267/267 tests, harness TODO OK, jsErrors []. 🟡 falta el ojo de Camilo en su celular: hacer pinch-zoom en el entreno guiado (timers/cabecera/botones fijos) y confirmar que nada se vea roto. NOTA de proceso: Camilo hace el diseño con Claude Design; yo implemento lo que aprueba (ver [[feedback_camilo_disena_yo_implemento]] en memoria). Auditoría descargada en Downloads/AVI Auditoría UX.pdf (4 P0, 6 P1, 6 P2, 8 quick wins); varios ya cubiertos (F4 = entreno protagonista; prompt de instalación PWA v201; backup auto semanal).*

*Hitos sesión 2026-07-04-e (F4 REGRESIÓN calentamiento 2ª pieza + GOTCHA de propagación de Pages, avi-v266): tras v265 (tarjeta de MOVILIDAD renderWarmup), Camilo reportó que SEGUÍA sin ver "el entrenamiento ni las series de aproximación ni el botón mostrar". DIAGNÓSTICO CLAVE (dejé de asumir caché): verifiqué PRODUCCIÓN con curl → servía avi-v264, NO v265; raw.githubusercontent.com/main → v265 correcto. O sea: el build de GitHub Pages de v265 quedó ATASCADO ~22 min (probablemente por 4 deploys seguidos v262→v265). NO era caché del usuario. Forcé rebuild con un commit extra (5f5c638) y v265 propagó. LECCIÓN: tras cada push VERIFICAR prod con `curl https://kronos-apex.github.io/apex-app/sw.js` (CACHE_NAME) y comparar con raw; si prod≠raw, el build está pendiente → commit extra lo re-lanza. NUNCA volver a decirle al usuario "es tu caché" sin haber confirmado qué sirve producción. SEGUNDA PIEZA REAL: "el botón mostrar" y "las series de aproximación" son distintos de la movilidad — son los "🔥 Sets de calentamiento" por ejercicio (buildWarmupSection en la clásica: header con toggle toggleExWarm→wshow_<rid>_<ei>→fila token w0). El guiado (gmRender) solo pintaba la fila si exWarmShown ya era true, SIN el botón para activarla. FIX: gmRender (peso_reps) ahora pinta el encabezado "🔥 Sets de calentamiento" con botón Mostrar/Ocultar (nuevo gmToggleExWarm(ei) → misma clave wshow_ y exWarmShown que la clásica, re-render del guiado) + la fila cuando está visible. Harness _repro-plancha.mjs S22c (encabezado con botón Mostrar; al tocarlo aparece la fila de aproximación y el botón pasa a Ocultar, wshow_rTest_0='1'). 267/267 tests, TODO OK, jsErrors []. QA inline. LECCIÓN (F5): al portar clásica→guiado se pasó por alto TODA la superficie de calentamiento (movilidad renderWarmup Y sets de aproximación buildWarmupSection); antes de F5 auditar que el guiado tenga TODO lo de renderClientToday+renderClientExList, no solo las tarjetas de ejercicio.*

*Hitos sesión 2026-07-04-d (F4 REGRESIÓN — el calentamiento/estiramiento faltaba en el guiado, avi-v265): Camilo reportó que en "Hoy" no aparecían los ejercicios de estiramiento, ni las reps de calentamiento, ni el botón "mostrar". Causa: al volverse el guiado el default (F4), "Hoy" es el guiado embebido (gmRender, app-6-extra.js), que NUNCA renderizó la sección de calentamiento — esa la pinta la CLÁSICA vía renderWarmup(#wu-wrap) (tarjeta colapsable "🔥 Calentamiento — {sesión}" con movilidad+activación auto-derivadas por buildWarmup, o routine.warmup custom del coach; cada fila con nombre, reps ("10 c/lado"/"30 seg"), botón 🎥 openWarmupDetail y ✓ wuToggle; toggle de colapso toggleWarmup con chevron ▼; persistencia wu_<rid>_<id> reseteada por día). El guiado solo tenía la fila "🔥 Calentamiento" de APROXIMACIÓN por ejercicio (exWarmShown, peso_reps) — otra cosa. FIX: gmRender ahora inserta <div id="wu-wrap"> tras gmRoutineHeaderHTML y antes de las tarjetas de ejercicio, y llama renderWarmup(GM.exercises) — reusa TODO el sistema clásico sin duplicar. Sin colisión de id: con el guiado ON la clásica no está montada (openGuidedEmbedded hizo con.innerHTML='' y renderClientToday retornó antes de su propio #wu-wrap), así que hay UN solo #wu-wrap en el DOM. Colapsado por defecto = paridad exacta con la clásica (el usuario toca el chevron para ver los estiramientos). Harness _repro-plancha.mjs +S22 (la tarjeta de calentamiento aparece en #gm-body con filas+reps+🎥+chevron; toggleWarmup alterna el estado). 267/267 tests, TODO OK, jsErrors []. QA inline (agentes julian/lucas sin sesión hasta 1:30pm Bogotá → fallback: node --check + tests + harness + revisión de colisión de id). LECCIÓN: al portar la clásica→guiado (F1/F2) se auditó paridad de EJERCICIOS pero se pasó por alto la sección de CALENTAMIENTO de sesión (no estaba en la matriz P1-P15 como ítem propio); al retirar la clásica (F5) verificar que el guiado tenga TODO lo que renderClientToday pintaba, no solo las tarjetas de ejercicio.*

*Hitos sesión 2026-07-04-c (F4 tooltip adaptado al guiado + respiración, avi-v264): Camilo pidió "adaptalo junto con los métodos de respiración que aparecen en el modo guiado" (el tooltip educativo "❓ ver video" que la auditoría v263 marcó como no-mostrado a usuarios nuevos en el guiado default). showExTooltip (app-6-extra.js) buscaba #cex-list de la clásica → en el guiado embebido (default F4) no existe → return sin mostrar nada. FIX: nueva gmShowExTip() pinta el tooltip DENTRO de #gm-body, sobre la 1ª .gm-ex-card, y ENSEÑA LAS DOS AYUDAS del guiado: el ❓ (video, openExDetail) + el 💨 "cómo respirar" (la respiración es P13, ventaja del guiado: breathCue/EX_BREATH + tarjeta gmShowStartCard "💨 Cómo respirar aquí" + recordatorio gm-rest-breath en el descanso). Texto: "Toca el ❓ de un ejercicio para ver el video. Y al empezar cada uno, el 💨 te muestra cómo respirar." showExTooltip ahora enruta: si _gmIsEmbedded() → gmShowExTip(); si no → clásica (#cex-list, intacta). gmRender llama gmShowExTip() al final → el tip se re-inserta mientras no esté cerrado (resiste el flujo ánimo-primero: aparece cuando ya se ven los ejercicios, aunque el chooser de ánimo vaya arriba) y NO molesta al marcar series (gmToggleSet hace update puntual, no gmRender). Helper _exTipClientId()=_obClientId||CUR.clientId (dismiss persiste en apex_tip_done_<cid> por dispositivo). Sin XSS: el innerHTML del tip es texto fijo. Harness _repro-plancha.mjs +S21 (tooltip guiado con ❓ video + 💨 respiración sobre la 1ª tarjeta; la × cierra y persiste tras re-render), REORDENADO antes de S20 porque S20b es DESTRUCTIVO del nodo #guided-mode compartido (openGuidedEmbedded mueve el nodo a #cn-today-body ANTES de gmRender; si gmRender lanza —#gm-body ausente— el try/catch cae a clásica y su innerHTML consume el nodo → guiado roto hasta recargar; solo pasa con SW-desync, aceptable). 267/267 tests, TODO OK, jsErrors []. QA: Julián 🟢 + Lucas 🟢.*

*Hitos sesión 2026-07-04-b (F4 BLINDAJE — auditoría profunda "tipo 5 agentes" + 2 guardas, avi-v263): tras encender el guiado por defecto (v262), Camilo pidió una auditoría profunda antes de irse a trabajar ("no quiero errores ni sorpresas"). 4 ángulos en paralelo: (1) DATOS REALES en Supabase (execute_sql sobre user_data.routines) — 497 ejercicios de 18 clientes+coach: 0 sin sets, 0 sets no-entero/cero, 0 sin id/name, 0 rutinas con exercises vacío; 7 tipos (Aislamiento/Compuesto/Bodyweight/Isométrico/Cardio/Funcional/HIIT) que exTrack (avi-core.js:1686, default seguro peso_reps) mapea a los 5 tracks manejados; HIIT real (e74) NO guarda hiit inline pero hiitCfg (app-4:792) cae a 30/15 → corre; Isométrico guarda segundos en reps y holdSecsOf (app-4:793) los lee o cae a 60; 4 ejercicios con id no-catálogo (fb01/fb04, custom del coach) son type Compuesto→peso_reps, renderizan bien. (2) COBERTURA: gmSetCellsHTML (app-6:500) tiene rama para los 5 tracks + el ○ de marcar existe siempre → ninguna celda muerta; cardio y biserie verificados (eran huecos del harness). (3) REGRESIÓN: _repro-back-v243.mjs 18/18 con perfil nuevo (guiado-default en el boot) sin errores JS; coach-self/startRoutineNow/override/updateClientProgress limpios. (4) ROBUSTEZ (hallazgo real): renderClientToday hacía con.innerHTML='' y luego openGuidedEmbedded SIN try/catch — el fallback solo cubría retorno falsy, NO un throw; un throw (p.ej. #gm-body ausente por SW/index viejo, o hueco null en exercises que hace lanzar a checkAndResetSession y a gmRebuild/guidedStepOrder) dejaba "Hoy" en BLANCO, y el poll de 15s lo dejaba PERMANENTE (client.routines ya asignado → el diff no vuelve a renderizar). Los datos reales NO lo disparan hoy (0 nulls), pero es trampa latente en un default para todos. FIX (2 guardas mínimas en app-4-entreno.js): (a) prepareTodaySession filtra huecos null de routine.exercises (raíz común de clásica y guiado; no-op sin huecos, no cambia índices en el caso sano); (b) try/catch alrededor de openGuidedEmbedded → CUALQUIER throw cae a la vista clásica de abajo (repinta con completo) en vez de blanco. Harness _repro-plancha.mjs +S20 (a: hueco null → guiado embebe los 2 reales sin blanco; b: sin #gm-body → openGuidedEmbedded lanza → cae a clásica cex-list, jsErrors []). 267/267 tests, TODO OK. QA: Julián 🟢 + Lucas 🟢. Hallazgos 🟡 NO aplicados (dejados para decisión de Camilo): showExTooltip (app-6:1062) busca #cex-list, ausente en el guiado default → el tooltip educativo "❓ ver video" no se muestra a usuarios NUEVOS (degrada sin crash, discoverability); y el harness no tiene assert del botón atrás parado sobre el tab embebido. Advisors de seguridad Supabase (RLS sin policy en apex_data/backups legacy, INSERT/UPDATE anón en push_subscriptions, leaked-password off) son PRE-EXISTENTES y ya conocidos, no de este cambio.*

*Hitos sesión 2026-07-04-a (unificación guiado F4 — GUIADO ENCENDIDO POR DEFECTO, avi-v262): Camilo dio el visto bueno tras probar F2+F3 en su celular → se desbloquea F4. El guiado embebido pasa a ser el DEFAULT de "Hoy": `uiGuided()` (app-1-infra.js) ahora lee `ld('ax_ui_guided','1')` (antes `'0'`). Semántica de la migración: quien NUNCA tocó el flag (sin clave en localStorage) → guiado; quien explícitamente guardó `'0'` con "↩ Volver a la vista clásica" → sigue en clásica (opt-out respetado, porque `ld` devuelve el valor guardado si existe). El enlace kill-switch del Perfil (`renderGuidedViewToggle`) queda VISIBLE en ambas direcciones ≥2 semanas. Fallback intacto: si `openGuidedEmbedded` no puede embeber, `renderClientToday` cae a la lista clásica de abajo. Harness `_repro-plancha.mjs`: S12 ahora verifica el DEFAULT REAL (`localStorage.removeItem('ax_ui_guided')` → `uiGuided()===true`, raw null) en vez de asumir OFF; y `setupRoutine` fija `setUiGuided(false)` explícito al inicio para que los escenarios clásicos (SETUP,S1–S2) y los del guiado por OVERLAY (S3–S11, openGuidedMode) sigan corriendo contra la clásica en "Hoy" — S12+ encienden/apagan el flag por escenario. Verificación 3 capas: `node --check` 3/3, 267/267 tests, `_repro-plancha.mjs` TODO OK (jsErrors []). 🟡 AHORA: Camilo + 1-2 clientes beta (Miguel/Kathe) USAN el default ~2 semanas; vigilar reportes (poll/foreground y TWA = frentes de riesgo). CERO reportes 2 semanas → habilita F5 (retiro de la clásica). Progreso en `docs/plan-unificacion-guiado.md` §3b.*
